# WM-0002 Work report

## Outcome

Bootstrapped the TypeScript pnpm monorepo skeleton for the architecture-approved apps, packages, and tools; added exact tool pins, a lockfile, strict TypeScript/lint/format/test gates, content and package-boundary validators, CI skeleton, and ADRs. No gameplay, simulation runtime, Worker protocol, UI behavior, Electron shell behavior, save format, or content compiler behavior was implemented.

## Files and public contracts changed

- `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `.npmrc`: root workspace, exact tool pins, package manager policy, root gate scripts, and lockfile.
- `tsconfig*.json`, `eslint.config.mjs`, `.prettierrc.json`, `.prettierignore`, `vitest.config.mjs`: strict TypeScript, lint, format, and unit-test configuration.
- `apps/web`, `apps/desktop-electron`: app workspaces with skeleton public `src/index.ts` APIs only.
- `packages/foundation`, `packages/content-schema`, `packages/content-compiler`, `packages/sim-core`, `packages/sim-protocol`, `packages/sim-worker`, `packages/renderer-pixi`, `packages/ui-react`, `packages/persistence`, `packages/platform`, `packages/testkit`, `packages/benchmarks`: package workspaces with explicit public entrypoints and `wumingBoundary.allowedInternalDependencies`.
- `tools/content-cli`, `tools/headless-runner`: tool workspaces with skeleton public APIs only.
- `tools/boundary-check.mjs`: manifest-level internal dependency guard plus TypeScript AST-based source import and deep-import guard.
- `tools/content-validation.mjs`: schema JSON parse check plus exact-version/workspace manifest validation.
- `.github/workflows/ci.yml`: CI skeleton running root quality gates; setup-node pnpm cache is intentionally disabled until pnpm is available on the runner.
- `coordination/decisions/ADR-0001.md`, `coordination/decisions/ADR-0002.md`, `docs/05_tech/07_architecture_decision_records.md`: monorepo boundary and pinned-tool decisions.
- `coordination/reports/WM-0002-plan.md`, `coordination/reports/WM-0002.md`: execution plan and final evidence.
- `coordination/tasks/WM-0002.json`: claimed WM-0002 as `systems-architect`, thread `Compass`, via `taskctl claim`.

## Acceptance evidence

- Workspace contains agreed apps/packages with enforceable dependency direction: all 16 architecture workspaces exist; `pnpm boundaries:check` validates manifest `wumingBoundary` declarations, declared internal dependency edges, source imports, and deep-import bans, and reports 16 workspaces.
- Typecheck, lint, unit-test and content-validation commands run from root: `pnpm typecheck`, `pnpm lint`, `pnpm test`, and `pnpm content:validate` all passed.
- Tool versions are pinned and documented in an ADR: exact devDependencies and `packageManager: pnpm@11.8.0` are in `package.json`/`pnpm-lock.yaml`; decisions are in `coordination/decisions/ADR-0002.md`.
- No product runtime behavior beyond minimal smoke test: only skeleton public API constants/helpers and one foundation smoke test were added; runtime framework installs are limited to Vite/Vitest tooling and no React/Pixi/Electron product code was imported or implemented.

## Checks run

| Command                                                                                                                            | Result           | Artifact / notes                                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------- |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate`                                                      | Pass             | `Validation passed: 13 task(s), 10 role(s).`                                                  |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status`                                                        | Pass             | WM-0002 left `in_progress`; unread inbox `0`.                                                 |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs claim --id WM-0002 --agent systems-architect --thread Compass` | Pass             | Task claimed; no `complete` run.                                                              |
| `pnpm install`                                                                                                                     | Pass after retry | First attempt timed out fetching Vite native binding; retry completed and generated lockfile. |
| `pnpm install --frozen-lockfile`                                                                                                   | Pass             | `Already up to date`; pnpm `11.8.0`.                                                          |
| `pnpm typecheck`                                                                                                                   | Pass             | `tsc --noEmit --project tsconfig.typecheck.json --pretty false`.                              |
| `pnpm lint`                                                                                                                        | Pass             | `eslint . --max-warnings 0`.                                                                  |
| `pnpm format:check`                                                                                                                | Pass             | Scoped to WM-0002-owned code/config/ADR/report files.                                         |
| `pnpm test`                                                                                                                        | Pass             | Vitest `1` file, `1` test passed.                                                             |
| `pnpm content:validate`                                                                                                            | Pass             | `Content validation passed for 16 workspaces.`                                                |
| `pnpm boundaries:check`                                                                                                            | Pass             | `Package boundary check passed for 16 workspaces.`                                            |
| `node tools/validate-handoff.mjs`                                                                                                  | Pass             | `Handoff validation passed: 245 files, 13 tasks, 10 roles.`                                   |
| `git diff --check`                                                                                                                 | Pass             | No whitespace errors.                                                                         |

## Determinism, save, performance and security impact

- Determinism: no simulation runtime, PRNG, true time, Worker protocol, or authoritative state behavior was added. `sim-core` lint restrictions now block `Math.random`, `Date.now`, `performance.now`, UI/Electron/Pixi imports, and Node file-system imports.
- Save: no save container, schema migration, or persistence behavior was added.
- Performance: no hot runtime loops were added; `sim-core` lint rules reject allocation-heavy array callbacks in that package.
- Security: external tool versions are exact and lockfile-backed; `.npmrc` enables exact saves, engine strictness, strict peers, and disabled auto peer installs. Electron/React/Pixi runtime dependencies were not introduced because no WM-0002 package consumes them.

## Decisions and deviations

- ADR-0001 accepts pnpm workspaces plus a manifest-driven package-boundary guard.
- ADR-0002 accepts exact installed tool versions and records deferred runtime framework candidates.
- Owner recheck adjusted CI to avoid setup-node pnpm cache before Corepack prepares pnpm, choosing the simplest reliable skeleton over extra cache behavior.
- Owner recheck strengthened `tools/boundary-check.mjs` so manifest-declared internal dependencies must be explicitly allowed and allowed internal dependencies must name known workspace packages.
- The format gate is intentionally scoped to WM-0002-owned files because existing handoff docs, inbox messages, and older reports are not globally Prettier-formatted.
- pnpm added `minimumReleaseAgeExclude` entries for the newly pinned packages while enforcing supply-chain policy; frozen install passes with the generated workspace file.
- No Spark was used for this architecture/bootstrap task.

## Remaining risks / follow-up tasks

- CI is a skeleton and has not run on GitHub from this branch inside this local session.
- CI actions use stable major tags; future release hardening can pin action SHAs.
- Future UI/Electron/Pixi tasks must add their runtime dependencies exactly when first imported and update dependency review evidence.
- Node is pinned to the Node 24 LTS line rather than a patch-level runtime binary.

## Handoff to reviewer

Start with `coordination/decisions/ADR-0001.md`, `coordination/decisions/ADR-0002.md`, `package.json`, `tools/boundary-check.mjs`, and `tools/content-validation.mjs`. Reproduce with `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm content:validate`, `pnpm boundaries:check`, `pnpm test`, `node tools/validate-handoff.mjs`, and `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate`. WM-0002 remains `in_progress`; per instruction, `taskctl complete` was not run.
