# WM-0047 Work report

## Outcome

Recorded ADR-0008 as the M3 ordinary-life architecture gate and added narrow
cross-links/notes in the relevant system, save/replay, observability,
performance, ADR index, and roadmap docs. The change is architecture-only: no
runtime implementation, tests, benchmarks, package manifests, lockfiles, public
Worker protocol, public save compatibility, schemas, dependencies, or M4
implementation were edited.

## Files and public contracts changed

- `coordination/decisions/ADR-0008.md`: new M3 architecture gate covering
  owner stores, derived indexes, caps, job-driver boundaries, ReasonTrace,
  save/replay, Worker parity, blockers, migration, rollback, tests, and
  performance.
- `docs/02_systems/05_needs_health_mood_social.md`: linked ADR-0008 and named
  M3 owner stores/blockers for needs, health, ability cache, mood, and
  relationships.
- `docs/02_systems/13_save_replay_migration.md`: linked ADR-0008 and clarified
  focused M3 save/replay scope, validation, rebuild, and non-authoritative
  derived caches.
- `docs/02_systems/14_explainability_debug_observability.md`: linked ADR-0008
  and clarified M3 ReasonTrace ownership, required row fields, and bounded
  capacity.
- `docs/05_tech/03_performance_budget.md`: added M3 metric surfaces and
  reaffirmed existing regression thresholds.
- `docs/05_tech/07_architecture_decision_records.md`: updated the ADR index to
  point ADR-0007 and ADR-0008 at the actual decision records.
- `docs/07_roadmap/08_m3_ordinary_life_simulation_plan.md`: noted that
  WM-0047 records ADR-0008 and that downstream M3 implementation must block on
  gate violations.
- `coordination/tasks/WM-0047.json`: task state changed through `taskctl claim`;
  the existing acceptance/allowed-path updates for ADR index coverage were
  preserved.
- `coordination/reports/WM-0047.md`: this report.

No public runtime contract changed. ADR-0008 explicitly blocks public Worker
protocol, public save/schema, dependency, and package-boundary changes unless a
separate reviewed gate approves them.

## Acceptance evidence

- ADR documents alternatives, chosen boundaries, migration, rollback, test, and
  performance implications: `coordination/decisions/ADR-0008.md`.
- Owner stores are named for needs, rest/sleep, food/eating, health
  conditions, abilities, medical care, mood/thoughts/memory, relationships,
  day/night, weather, random streams, jobs/reservations, ReasonTrace,
  save/replay, and Worker read models: ADR-0008 ownership table and contracts.
- Derived indexes, dirty queues, candidate caps, Top-K limits, versioned
  invalidation, and structured ReasonTrace ownership are specified: ADR-0008
  derived index/cap and ReasonTrace sections.
- Serializable job-driver boundaries are specified for rest, eating, and
  treatment jobs: ADR-0008 rest/eating/treatment job-driver section.
- ADR index is updated to point ADR-0008 at the M3 ordinary-life architecture
  gate: `docs/05_tech/07_architecture_decision_records.md`.
- Block conditions are explicit for owner-store gaps, global scans,
  unversioned caches, public Worker protocol changes, public save/schema
  changes, new dependencies, package-boundary exceptions, UI authority, real
  time/random, Promise/coroutine jobs, and M4 scope: ADR-0008 downstream block
  conditions.
- No forbidden runtime, package, protocol, schema, dependency, test,
  benchmark, or M4 implementation paths were edited: verified by diff
  inspection and required checks below.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `node tools/validate-handoff.mjs` | Passed | `Handoff validation passed: 653 files, 60 tasks, 10 roles.` |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Passed | `Validation passed: 60 task(s), 10 role(s).` |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | Passed | Task states at check time: `proposed: 13`, `in_progress: 1`, `done: 46`; unread inbox messages `0`. |
| `git diff --check` | Passed | No whitespace errors. |
| `pnpm quality` | Passed | Ran typecheck, lint, format check, content validation, package boundary check, and unit tests. `25` test files and `133` tests passed. |

## Determinism, save, performance and security impact

Determinism: ADR-0008 preserves fixed 30 TPS, seeded named streams, stable
ordering, integer/fixed-point semantics, bounded indexed thinking, and
serializable jobs.

Save/replay: focused M3 save/replay scope is documented; public save
compatibility, public schema migration, platform save UI, codec dependencies,
and persisted derived caches are explicitly blocked.

Performance: ADR-0008 and the performance doc name M3 candidate caps, Top-K
limits, dirty queue metrics, cache invalidation metrics, save rebuild metrics,
Worker parity metrics, and the existing 10 percent warning / 20 percent
blocking regression thresholds.

Security and authority: UI, Pixi, React, Electron, protocol adapters, platform
save code, read models, real time, ambient randomness, and hidden shared state
remain non-authoritative. No new dependency or public attack surface was added.

## Decisions and deviations

- New decision: ADR-0008 proposes store-owned ordinary-life facts with
  versioned derived indexes and bounded selection as the M3 architecture gate.
- Rejected alternatives: monolithic life brain, UI-owned life/status state,
  persisted derived caches for resume, and incidental public contract drift.
- Spark was not used; WM-0047 is explicitly not Spark-eligible because it is an
  architecture/state ownership/save-replay/Worker parity gate.
- No scope deviation from allowed paths.

## Remaining risks / follow-up tasks

- ADR-0008 remains Proposed until independent review; downstream M3
  implementation must stay blocked if the reviewer rejects or supersedes it.
- Downstream tasks must prove the named owner stores and caps with tests and
  benchmark artifacts; this task does not implement those surfaces.
- If an implementation task needs public Worker protocol, public save/schema,
  dependency, package-boundary, UI authority, real-time/random authority,
  Promise/coroutine job state, or M4 scope, it must block and request a separate
  reviewed gate.

## Handoff to reviewer

Fastest review path:

1. Read `coordination/decisions/ADR-0008.md`.
2. Check the narrow doc notes in the files listed above.
3. Confirm the diff contains only allowed architecture/doc/report/task metadata
   paths.
4. Re-run the required checks listed in this report.
