# WM-0178 Work report

## Outcome

Canonical fixed-baseline gate restored on the repaired-main lineage. After
WM-0179 was independently verified, integrated and done, systems architecture
refreshed this branch at `dba8e88` with exactly the four reviewed benchmark
harness/test files and authorized one final campaign.

The reviewed production admission ran exactly once and passed two six-second
windows at 21.631308 and 19.371789 percent host busy, with maximum process
shares of 6.201191 and 6.262081 percent. It emitted one measured PASS JSON line,
zero stderr bytes and exit zero; commits, clean-tree facts, `ACShadows` absence
and the external no-benchmark confirmation all matched.

Each of the six required filters then ran exactly once in task order, followed
by exactly one unfiltered `corepack pnpm bench`. Every command exited zero,
used 9 samples and 2 warmups, matched all invariants and wrote a verified JSON
artifact plus SHA-256 sidecar to a unique external temporary directory before
byte-identical import under `final-repaired-main/`. The full suite reports 15
OK, zero warnings, zero failures and zero invariant mismatches. No admission or
benchmark was retried, and no baseline, threshold, workload or historical
artifact changed.

## Files and public contracts changed

- `packages/benchmarks/src/cli-lib.ts`: run each suite in a separate Node
  process; validate child schema, finite samples, sample/warmup counts,
  environment/commit identity, exit status, parent-baseline comparison,
  canonical payload and sidecar before aggregating; recompute all five sample
  statistics from the raw samples before trusting the child result; add command,
  exit, process, host-key and hash evidence.
- `packages/benchmarks/src/benchmarks.ts`: expose the immutable 15-suite default
  order used by the isolated parent runner and export the existing statistics
  implementation for exact sibling-module validation.
- `packages/benchmarks/src/index.ts`: export the additive runner metadata types
  and child-artifact validator.
- `packages/benchmarks/src/baseline.test.ts`: cover a real isolated child,
  distinct PID, raw/hash metadata, a full-shaped valid child, malformed
  statistics and every finite raw/statistics mismatch fail-closed behavior.
- `coordination/artifacts/WM-0178/**`: raw paired and repaired-lineage JSON plus
  their generated SHA-256 sidecars and host-load evidence.

The JSON artifact keeps `schemaVersion: 1` and adds fields. This is treated as
an additive compatible extension because existing comparison payloads and
result fields are unchanged; consumers that reject unknown fields require
independent review before integration.

## Acceptance evidence

### Immutable gate audit

- `packages/benchmarks/baseline.json` Git blob remained
  `f67c942269b41c023d236749a3332c4ae3bfbd17`.
- Historical WM-0083 JSON and sidecar Git blobs remained
  `e92caaf1ff187dcb5f128be5f61cd4162f144863` and
  `cddf88c33338e60037c8a2b509b9b3cc819e7595` after every comparison.
- `DEFAULT_BENCHMARK_SAMPLE_COUNT` remains `9` and
  `DEFAULT_BENCHMARK_WARMUP_COUNT` remains `2`.
- All 15 baseline entries still carry warning `10` and failure `20`; no
  workload, fixture or invariant payload changed.

### Clean paired attribution before repair

Every command began from a clean detached checkout and wrote only to an
external temporary artifact directory. Suite order alternated as approved.
Raw samples and generated sidecars are under
`coordination/artifacts/WM-0178/pre-repair-paired/`.

| Order | Suite | Commit | Median ms | Gate / exit |
|---:|---|---|---:|---|
| 1 | entity-store | `4175afd` | 444.6516 | FAIL / 1 |
| 2 | entity-store | `054d501` | 456.9323 | FAIL / 1 |
| 3 | M3 ordinary life | `054d501` | 37.5923 | FAIL / 1 |
| 4 | M3 ordinary life | `4175afd` | 34.0029 | FAIL / 1 |
| 5 | M4 vertical slice | `4175afd` | 8.5790 | FAIL / 1 |
| 6 | M4 vertical slice | `054d501` | 9.0250 | FAIL / 1 |
| 7 | M5 alpha content | `054d501` | 82.0934 | OK / 0 |
| 8 | M5 alpha content | `4175afd` | 90.3287 | FAIL / 1 |
| 9 | map-dirty | `4175afd` | 1.2945 | FAIL / 1 |
| 10 | map-dirty | `054d501` | 1.3347 | FAIL / 1 |
| 11 | spatial-index | `054d501` | 1608.7247 | WARN / 0 |
| 12 | spatial-index | `4175afd` | 1622.8928 | WARN / 0 |
| 13 | full suite | `4175afd` | raw 15-suite payload | FAIL / 1 |
| 14 | full suite | `054d501` | raw 15-suite payload | FAIL / 1 |

The pre-repair full runs failed five suites each. The M5 order reversal versus
the earlier reviewed attribution, together with the wide within-suite samples,
shows that this host run cannot assign all deltas to WM-0177 product changes.
It does not invalidate the earlier evidence; it records why a new exact
product attribution cannot be made under the current host load.

### Harness repair proof

- Targeted test artifact: the parent mode is
  `isolated-node-per-suite`; the child mode is `isolated-suite-child`.
- A full report contains 15 unique child PIDs in the immutable 15-suite order.
- Parent code reloads the unchanged baseline and recomputes every child
  comparison. A child is rejected before aggregate write if its suite name,
  raw finite sample count, warmup/sample metadata, commit, Node/pnpm/OS/CPU/host
  identity, exit code, comparison, actual-file hash, sidecar or canonical hash
  differs.
- Child temporary artifacts are deleted only after validation. The final parent
  artifact is not written until every child has passed the transport checks.
- No affinity, priority, retry, fallback result or failure suppression was
  added.

### Review-requested sample-statistics integrity repair

The independent reviewer reproduced a P2 integrity gap with a diagnostic copy
of the final `entity-store` payload: the raw median remained 1.419 ms while the
reported median was changed to 0, yet the old parser accepted the child and the
parent recomparison became -100 percent / OK. No tracked evidence was modified
by that diagnostic.

- `createBenchmarkStats` is now exported from `benchmarks.ts` and imported by
  the sibling parser through `./benchmarks`; it was not added to the package
  index and no second statistics algorithm was introduced.
- After finite raw samples and exact sample count are validated, the parser
  requires an integer finite `sampleCount` plus finite min, median, max and mean.
  Missing, null, string or JSON-normalized non-finite values fail with
  `benchmark child artifact for <suite> has invalid sample statistics`.
- The parser recomputes the statistics once with the shared implementation and
  compares all five fields with `Object.is`, preserving signed-zero semantics.
  A finite mismatch fails with
  `benchmark child artifact for <suite> sample statistics do not match raw samples`.
- Recalculation occurs in the parent after the child artifact has been emitted,
  outside every measured benchmark sample region. It cannot change the stored
  samples, medians, workload, threshold, order or sample/warmup counts.
- Focused coverage includes one full-shaped valid child, five individual finite
  mismatches, string/null/JSON-non-finite malformed statistics, and the exact
  raw 1.419 / reported 0 reviewer diagnostic.
- A read-only audit loaded the same exported algorithm and checked all 31
  WM-0178 JSON/sidecar pairs. All sidecars matched; all 29 benchmark artifacts
  and 99 results had valid schema/raw counts and exact five-field statistics;
  the seven `final-repaired-main` artifacts contained 21 OK results. SHA-256
  values for all 62 audited files were unchanged before and after the audit.
  No admission or benchmark command was rerun.

### Single repaired-lineage verification

All commands ran exactly once at `7434d6f` with Node `v24.13.1`, pnpm `11.8.0`,
Windows `10.0.26200`, x64, AMD Ryzen 7 9700X (16 logical processors), host key
`55442F953FEA3046AA652002ACBA5F947F40C7B767FBE51A63885BCC49E95912`, 9
samples and 2 warmups. Raw JSON and sidecars are under
`coordination/artifacts/WM-0178/repaired-main/`.

| Command | Median / result | Exit |
|---|---|---:|
| `corepack pnpm bench --filter entity-store` | 409.761 ms, +67.59%, FAIL | 1 |
| `corepack pnpm bench --filter m3-ordinary-life-long-run` | 35.248 ms, +44.13%, FAIL | 1 |
| `corepack pnpm bench --filter m4-core-vertical-slice-long-run` | 8.405 ms, +82.28%, FAIL | 1 |
| `corepack pnpm bench --filter m5-alpha-content-long-run` | 104.950 ms, +40.62%, FAIL | 1 |
| `corepack pnpm bench --filter map-dirty` | 2.000 ms, +100.02%, FAIL | 1 |
| `corepack pnpm bench --filter spatial-index` | 1645.717 ms, +14.55%, WARN | 0 |
| `corepack pnpm bench` | 3 FAIL, 2 WARN, 10 OK | 1 |

The repaired full run recorded:

- `entity-store`: 436.246 ms, +78.42%, FAIL;
- M3: 27.679 ms, +13.18%, WARN;
- M4: 6.965 ms, +51.05%, FAIL;
- M5: 70.856 ms, -5.06%, OK;
- `map-dirty`: 1.436 ms, +43.61%, FAIL;
- `spatial-index`: 1615.682 ms, +12.46%, WARN;
- every other suite OK and every invariant matched.

The filtered/full differences, particularly M3 and M5, remain scheduling
sensitive despite process isolation. The residual `entity-store`, M4 and
`map-dirty` failures prove only that suite isolation is insufficient; they do
not prove a product regression while the host is not quiescent.

### Prior host-load stop condition

`coordination/artifacts/WM-0178/host-cpu-load.json` records aggregate CPU
samples of 89.40-94.88 percent before repair, 86.46-94.01 percent after bounded
diagnostic cleanup and 74.93-85.43 percent after the repaired run. An
independent process-counter sample reported aggregate `_total=1503.74` on 16
logical processors with only `476.87` idle; the largest consumers were Codex,
ACShadows, VS Code, Git and ChatGPT. No residual benchmark Node process was
present.

That prior external stop condition was cleared on 2026-07-12 when the user
closed `ACShadows`. Systems architecture authorized one bounded full-suite
resume only after two quiet-host admission windows; the result is recorded
below. The historical high-load evidence remains useful for differentiating
load-sensitive failures from the persistent residual.

### Quiet-host resume

- Admission window 1: 24.41 percent host busy, 6.18 percent maximum process
  share (`Code`, PID 40096), `ACShadows` absent, zero benchmark Node processes,
  clean worktree.
- Admission window 2: 22.53 percent host busy, 6.14 percent maximum process
  share (`Code`, PID 40096), `ACShadows` absent, zero benchmark Node processes,
  clean worktree.
- Command: exactly one `corepack pnpm bench`, unchanged harness, 9 samples and
  2 warmups, full 15-suite order, exit 1.
- Raw artifact:
  `coordination/artifacts/WM-0178/quiet-host-resume/benchmarks/benchmark-results.json`.
  Its SHA-256 is
  `3E9D3347D07DE219345166F187A5817E50F82DD6D6F4999A475316EA893CDCCB`;
  the generated sidecar contains the same payload-file hash and the sidecar
  file SHA-256 is
  `DF70E20E3A7D3AF7AAA89628A71BE90695A5777EEA065EA460278B88449129E6`.
- Residual comparison: the prior repaired full run had `entity-store` +78.42
  percent FAIL, M4 +51.05 percent FAIL and `map-dirty` +43.61 percent FAIL. The
  quiet full has `entity-store` +45.54 percent FAIL, M4 +1.43 percent OK and
  `map-dirty` +10.89 percent WARN. M3, M5 and `spatial-index` are OK. This is a
  different residual pattern with one persistent cross-environment failure.
- The canonical baseline SHA-256 remains
  `CB1A50BC1EECFC4AC308722461F7B606F42DB38CB63336FDC7B4E58E9ACDAD15`.
  Historical WM-0083 JSON and sidecar SHA-256 values remain
  `04DB70ECD54022C298293BE9B00EDF404AC18122742F3ACB0C17AC21EE58D346`
  and
  `579BD72F1B438E1AF7A3BA192696B461881210FFC40530C9180CBD86F673BED6`.

### Final repaired-main campaign

The final campaign ran at exact commit
`dba8e88b4dc8936f95b050d0bb0e195063999bb2`. Before and after admission and
every benchmark, the tree was clean, the HEAD stayed exact, canonical baseline
blob stayed `f67c942269b41c023d236749a3332c4ae3bfbd17`, WM-0083 tracked tree stayed
`deaf9de87fe5988ba7a0a0979f1724b055bac9fd`, and the historical result/sidecar
blobs stayed `e92caaf1ff187dcb5f128be5f61cd4162f144863` and
`cddf88c33338e60037c8a2b509b9b3cc819e7595`.

Admission evidence is
`coordination/artifacts/WM-0178/final-repaired-main/admission.json`, SHA-256
`66AD2D39BD8FEA34AABA331F8D36CD30D9E9D0E32ABC3E24FFEDAD02639D747C`.
It records PowerShell `5.1.26100.8655`, 16 logical processors, locked constants
2 windows / 6 seconds / 40 percent host / 8 percent max process, exactly two
finite passing windows, maximum process `Code` PID 40096 at 6.262081 percent,
and all required repository/process facts true.

The measured admission line was emitted with CRLF. The nested
`final-repaired-main/.gitattributes` contains exact
`admission.json -text -diff`: `-text` preserves the original measured bytes
across staging and future checkout, while `-diff` prevents the preserved CR
from being misreported as trailing whitespace. All 16 imported
evidence/sidecar files therefore retain their original measured bytes. The
first pre-attribute cached index entry
remained blob `e5fde6176fabf69b2bcc7a345d88a2f77ce4f2c1`, whose binary export had the
rejected normalized SHA-256
`A933568963B46FF03588A571676D4BE4F7E3BE622CA5EF87D25AC25EA48078E4`.
The owner blocked instead of committing it; systems architecture acknowledged
the blocker and restored `review_requested` with one authorized porcelain
renormalization. That operation produced accepted raw blob
`d6646c860551a476cfebccf90af95d75fe1f1322`, whose staged binary export,
worktree and sidecar all hash to
`66AD2D39BD8FEA34AABA331F8D36CD30D9E9D0E32ABC3E24FFEDAD02639D747C`.
A subsequent ordinary `git add` kept the same OID and SHA-256.

| Order | Command | Median / regression | Gate | JSON SHA-256 |
|---:|---|---:|---|---|
| 1 | `corepack pnpm bench --filter entity-store` | 1.4190 ms / -99.42% | OK | `D5697E475AFF3CB570FC333B506EE037EB3CF18382524369253CB7B01ACBB671` |
| 2 | `corepack pnpm bench --filter m3-ordinary-life-long-run` | 17.8173 ms / -27.15% | OK | `41D0B089732D90DDDD40DC61A3C575F2DD8DE2538039F480ABA3FF821EC1945B` |
| 3 | `corepack pnpm bench --filter m4-core-vertical-slice-long-run` | 4.7070 ms / +2.08% | OK | `3E8DE015500D60B4981F266B53AACA1BB2D3098BBA5344E9C8D5CD6C04F50D7A` |
| 4 | `corepack pnpm bench --filter m5-alpha-content-long-run` | 58.2728 ms / -21.92% | OK | `186F48307986E50D71A19FEDF5CC38AEB99787A7A9B14F754D2B3E8561DA18DC` |
| 5 | `corepack pnpm bench --filter map-dirty` | 0.9001 ms / -9.99% | OK | `891C38E1ABFB9AAE0C275AFB1A4BD083794C4F157A06AD41D9FF9371D22E1A52` |
| 6 | `corepack pnpm bench --filter spatial-index` | 1475.2013 ms / +2.68% | OK | `12A6DD3D25FAE91E05E685AE5DB9A3C5F056AF17DBF52A603B08416FB55E3DED` |
| 7 | `corepack pnpm bench` | 15 OK / 0 WARN / 0 FAIL | OK | `7E145BCE895F664C961B044B5CF9F2164E27B5C8CB3430FA652CC03F82013BC4` |

The final full-suite medians for the six reviewed slices are: `entity-store`
1.5170 ms (-99.38%), M3 16.9284 ms (-30.78%), M4 4.0735 ms (-11.66%), M5
49.5881 ms (-33.56%), `map-dirty` 0.9213 ms (-7.87%) and `spatial-index`
1476.8459 ms (+2.79%). All 15 comparisons are OK.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `corepack pnpm typecheck` | PASS | final pre-benchmark run |
| `corepack pnpm exec vitest run packages/benchmarks/src/baseline.test.ts packages/benchmarks/src/map-dirty-benchmark.test.ts` | PATH unavailable | `vitest` was not resolved by pnpm on this PowerShell host |
| `.\\node_modules\\.bin\\vitest.CMD run packages\\benchmarks\\src\\baseline.test.ts packages\\benchmarks\\src\\map-dirty-benchmark.test.ts` | PASS | locked equivalent, 2 files / 6 tests |
| six required filtered benchmark commands | 1 WARN pass, 5 failures | raw artifacts above; no retries |
| `corepack pnpm bench` | FAIL | 3 failures, 2 warnings, 10 OK; all invariants match |
| `git diff --check` | PASS | before implementation checkpoint |
| quiet-host `corepack pnpm bench` | FAIL | exactly once at `fe4e8f`: `entity-store` only FAIL; `map-dirty` WARN; 13 OK; all invariants match |
| final reviewed quiet-host admission | PASS | exactly once; measured PASS JSON, exit 0, one stdout line, zero stderr |
| final six required filtered benchmarks | PASS | each exactly once in task order; all OK, 9/2, zero invariant mismatches |
| final `corepack pnpm bench` | PASS | exactly once; 15 OK, 0 WARN, 0 FAIL, zero invariant mismatches |
| final `corepack pnpm typecheck` | PASS | strict workspace typecheck |
| final focused benchmark tests | PASS | exact required Vitest command, 2 files / 6 tests |
| final `corepack pnpm boundaries:check` | PASS | 16 workspaces |
| final `node tools/validate-handoff.mjs` | PASS | 1739 files, 180 tasks, 10 roles |
| final taskctl validate/status | PASS | 180 tasks / 10 roles; WM-0178 sole in-progress task |
| final `git diff --check` | PASS | no whitespace errors |
| final `corepack pnpm quality` | PASS | 78 unit files / 466 tests plus type, lint, format, localization, content, boundaries and M7 checklist |
| review repair `corepack pnpm typecheck` | PASS | strict workspace typecheck |
| review repair scoped ESLint | PASS | three authorized benchmark source/test files, zero warnings |
| review repair focused benchmark tests | PASS | exact required Vitest command, 2 files / 15 tests |
| review repair read-only artifact audit | PASS | 31 JSON/sidecar pairs, 29 benchmark artifacts / 99 results, final 7 artifacts / 21 OK results, 62 files unchanged |
| review repair `corepack pnpm boundaries:check` | PASS | 16 workspaces |
| review repair `node tools/validate-handoff.mjs` | PASS | 1744 files, 180 tasks, 10 roles |
| review repair taskctl validate/status | PASS | 180 tasks / 10 roles; WM-0178 sole in-progress task; zero unread messages |
| review repair `git diff --check` | PASS | no whitespace errors before full quality |
| review repair `corepack pnpm quality` | PASS | 78 unit files / 475 tests plus type, lint, format, localization, content, boundaries and M7 checklist |

The earlier non-zero campaign remained a hard stop and was not retried. The
final campaign is a distinct systems-architect-authorized repaired-main run
after WM-0179 closure; every authorized command was invoked once only.

## Determinism, save, performance and security impact

- Determinism: invariant payloads matched in every paired and repaired run; no
  simulation state or iteration order changed.
- Save: no save format, codec, migration or owner state changed.
- Performance: measurement workloads and gate thresholds are unchanged. The
  isolated runner plus integrated WM-0179 structural-ordering repair now makes
  the canonical full gate green: 15 OK, no warning or failure.
- Security: children use `process.execPath`, the real `import.meta.url` CLI and
  loader paths, `shell: false`, fixed arguments and inherited environment plus
  one private recursion marker. No new dependency, network or product surface
  was added.

## Decisions and deviations

- ADRs and product authority were not changed.
- The earlier campaign recorded a focused Vitest PATH failure and used the
  repository-locked `vitest.CMD` equivalent. In the final campaign the exact
  required `corepack pnpm exec vitest ...` command resolved and passed 2 files /
  6 tests.
- Raw benchmark artifacts were first directed outside the repository so every
  comparison began clean and no historical artifact was overwritten. They were
  copied byte-for-byte into WM-0178 only after measurement.
- Artifact schema version remains `1` with additive metadata; this compatibility
  judgment needs independent review if the task later resumes.
- The first final quality pass exposed pre-existing lint failures in the
  reviewed harness diff; the next exposed formatting in the same two authorized
  files. The test now narrows its parsed fixture as `unknown`; one unnecessary
  erased assertion was removed from the child-result construction; and the
  existing child-report return keeps its already reviewed runtime checks plus a
  line-scoped lint suppression adjacent to the type-only assertion. No
  executable benchmark branch or function changed after evidence collection.
  The two files were formatted, and scoped lint/type/focused checks plus the
  final full quality gate passed without rerunning a benchmark.
- The first post-evidence quality unit run under the 78-file parallel load
  measured the test-only one-sample `empty-tick` probe at 10.580 ms / +29.40%.
  The unchanged runner correctly failed closed with exit 1, exposing that the
  isolation-metadata test incorrectly hard-coded exit 0. Keystone authorized a
  test-only correction: comparison status is narrowed to `ok|warn|fail`, and
  the direct return, artifact invocation and suite-process exits must all equal
  the status-derived expected exit. No runner, baseline, workload, threshold or
  collected evidence changed, and no benchmark command was rerun.
- Independent review then found that child statistics were shape-checked but
  not tied to their finite raw samples. The repair uses the existing shared
  statistics function, rejects malformed and mismatched fields with distinct
  errors, and adds fail-closed regression coverage. The read-only 31-pair audit
  proves all stored evidence remains compatible and byte-identical; no
  performance or admission command was rerun for this repair.
- The first review-repair full quality pass stopped only at Prettier for
  `baseline.test.ts` and `cli-lib.ts`; typecheck and global ESLint had already
  passed. Those two authorized files were formatted mechanically, scoped
  type/lint/focused tests passed again, and the complete quality chain then
  passed. No runtime behavior or performance evidence changed.

## Remaining risks / follow-up tasks

The performance blocker is resolved by the already reviewed/integrated WM-0179
repair and the final evidence above. WM-0177 and WM-0170 remain blocked until
WM-0178 completes independent review, integration and `done`; this worker does
not unblock either downstream task.

## Handoff to reviewer

Review the harness/test diff, especially the shared raw-sample/statistics
validation and its distinct malformed-versus-mismatch errors, historical paired
evidence, admission JSON and seven final artifact/sidecar pairs. Confirm each
final command ran once, all 15 full-suite comparisons and invariants pass, the
baseline and WM-0083 tracked blobs remain exact, the 31-pair compatibility audit
is read-only, and no waiver/retry or downstream unblock occurred.
