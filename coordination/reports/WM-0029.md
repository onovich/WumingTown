# WM-0029 Work report

## Outcome

Completed the M1 benchmark suite and long-run invariants closeout for
hauling/building without starting WM-0030. The benchmark suite now includes a
focused `m1-hauling-building-long-run` entry, the task-scoped benchmark artifact
records runtime environment metadata, `pnpm test --filter m1-invariants` covers
the 100k-tick invariant gate, and the documented `pnpm sim:run -- --seed 1
--scenario hauling-building --ticks 100000` invocation now works through the
headless CLI. The repaired long-run evidence path samples the idle window from
tick `2401` through `100000`, so queue-growth and stale-reference findings come
from repeated post-terminal observations rather than a single terminal-state
read.

## Files and public contracts changed

- `packages/benchmarks/src/m1-hauling-building-long-run-benchmark.ts`:
  task-scoped 100k-tick hauling/building benchmark with replay/save/hash and
  leak/no-growth invariants.
- `packages/benchmarks/src/benchmarks.ts`,
  `packages/benchmarks/src/baseline.ts`,
  `packages/benchmarks/src/index.ts`,
  `packages/benchmarks/src/cli-lib.ts`,
  `packages/benchmarks/baseline.json`: benchmark registry/baseline wiring,
  explicit WM-0029 artifact path, environment metadata capture, reviewed
  `m1-hauling-building-long-run` baseline entry and explicit `map-dirty`
  canonical hash refresh to `0xba7253ca` with thresholds unchanged at 10/20.
- `packages/sim-core/src/m1-invariants.test.ts`: focused long-run invariant
  gate covering reservation leaks, stale offers, negative resources, save
  round-trip and replay/hash divergence diagnostics.
- `packages/sim-core/src/hauling-building-scenario.ts`: scenario summary now
  records idle-window sample count, state hash stability and maximum offer,
  reservation and job counts from the post-terminal long-run window.
- `packages/sim-core/src/build-site.test.ts`: scenario fixture regression
  coverage for idle-window no-growth/no-stale-reference evidence.
- `tools/test-runner.mjs`: adds the `m1-invariants` unit filter.
- `tools/headless-runner/src/index.ts`,
  `tools/headless-runner/src/index.test.ts`: accepts the forwarded pnpm `--`
  separator and adds the minimal regression test for the documented `sim:run`
  invocation.
- `docs/05_tech/03_performance_budget.md`,
  `docs/05_tech/05_testing_observability_ci.md`,
  `docs/06_engineering/05_testing_policy.md`,
  `docs/07_roadmap/06_m1_simulation_kernel_plan.md`: WM-0029 benchmark,
  invariant, artifact-metadata and roadmap notes.
- `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json`: reviewed
  benchmark artifact with environment metadata and baseline comparisons.

No standalone changelog file exists in this workspace, so this report and the
updated technical docs record the WM-0029 change log entry.

## Acceptance evidence

| Acceptance criterion | Evidence |
|---|---|
| Benchmarks cover dirty map rebuild, spatial index, 100 paths, 10k logistics, reservations and hauling/building long run | `pnpm bench` passes with `empty-tick`, `entity-store`, `logistics-10k`, `m1-hauling-building-long-run`, `map-dirty`, `pathing-100`, `reservations`, `region-room`, `spatial-index` and `work-offers`; task artifact at `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json`. |
| Long-run checks cover reservation leaks, stale entity references, negative resources, queue growth, save round trip and hash divergence | `packages/sim-core/src/m1-invariants.test.ts` runs the hauling/building scenario to `100000` ticks, compares replay/save-resume hashes and asserts `idleWindow.sampleCount=11`, `idleWindow.hashStable=true`, zero active offers, zero active reservations, zero running jobs and non-negative resource counts. |
| Baseline updates are explicit artifacts with 10 percent warning and 20 percent blocking thresholds preserved | `packages/benchmarks/baseline.json` adds `m1-hauling-building-long-run`, including idle-window sample invariants, and refreshes `map-dirty` to the reviewed `0xba7253ca` artifact value while keeping `warnRegressionPercent=10` and `failRegressionPercent=20`. |
| Reports include Node, pnpm, OS, CPU, commit and scenario seed metadata | `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json` records `nodeVersion`, `pnpmVersion`, `osRelease`, `platform`, `arch`, `cpuModel`, `cpuCount` and `gitCommit`; the long-run benchmark invariants include `scenarioId`, `scenarioSeed`, `idleSampleCount`, `idleMaxActiveOfferCount`, `idleMaxActiveReservationCount` and `idleMaxRunningJobCount`. |

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `pnpm format:check` | Pass | Prettier check passed before closeout |
| `pnpm lint` | Pass | Re-run after headless CLI/test changes |
| `pnpm typecheck` | Pass | Re-run after idle-window repair |
| `pnpm exec vitest run tools/headless-runner/src/index.test.ts` | Pass | 1 test passed; guards forwarded `--` handling |
| `pnpm test --filter m1-invariants` | Pass | 1 test passed after idle-window repair |
| `pnpm bench` | Pass | `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json`; all benchmark comparisons `OK`, including idle-window invariants |
| `pnpm sim:run -- --seed 1 --scenario hauling-building --ticks 100000` | Pass | `finalWorldHash=0xf7815189`, `completedBuildingCount=1`, `idleWindow.sampleCount=11`, long-run stable |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | Re-run after idle-window repair and report updates |
| `git diff --check` | Pass | Re-run after idle-window repair and report updates |

## Determinism, save, performance and security impact

Determinism: the new benchmark and invariant test use fixed seed `1`, fixed tick
targets and existing replay/save helpers; no ambient randomness or real time was
introduced into `sim-core`. Save/replay: WM-0029 validates the focused M1 save
round-trip path and divergence diagnostics but does not expand save format scope.
Performance: the benchmark suite remains artifact-backed and thresholded at
10/20 without weakening regression policy; the `map-dirty` hash refresh is an
explicit artifact-backed baseline correction, not a looser threshold. Security:
no new runtime dependency, browser authority, DOM access, Node filesystem access
inside `sim-core` or protocol surface was introduced.

## Decisions and deviations

- Execution handoff: the initial qa-performance executor Sentinel produced no
  output and was no longer relied on; Gauge had a first no-response session
  closed; the active qa-performance thread took over after a successful probe of
  the WM-0029 worktree and workflow validation.
- A temporary dispatch attempt was abandoned and produced no output.
- Review repair during closeout: `tools/headless-runner/src/index.ts` now
  ignores a standalone `--` so the required pnpm forwarding form works exactly
  as documented, and `tools/headless-runner/src/index.test.ts` locks that
  behavior with a minimal regression test.
- Review repair after `changes_requested`: the scenario summary and benchmark
  now sample the tick `2401` to `100000` idle window and source
  `noQueueGrowth` / `noStaleEntityReferences` from repeated offer, reservation
  and job metrics.
- Coordination note: the active qa-performance thread diagnosed the repair and
  provided the exact patch plan, but its editor tooling repeatedly failed to
  land the patch. Beacon mechanically applied that bounded repair plan and kept
  final acceptance with the independent reviewer.
- WM-0029 stays inside benchmark/invariant/docs scope. No WM-0030 closeout work,
  no product expansion and no threshold weakening were started.

## Remaining risks / follow-up tasks

- `coordination/thread-registry.json` carries the active qa-performance thread
  registration update from in-flight task work; it is workflow metadata rather
  than product behavior.
- The benchmark artifact captures the current pre-commit Git revision in its
  environment block; later reruns on reviewer or integration branches will
  naturally record different commit ids.
- WM-0030 remains responsible for full milestone closeout, integrated gate
  review and residual-risk signoff.

## Handoff to reviewer

Review from `D:\WebProjects\WumingTown-WM-0029` on branch
`task/WM-0029-add-m1-benchmark-suite-and-long-run-invariants`.

Fast path:

1. Inspect `packages/benchmarks/src/m1-hauling-building-long-run-benchmark.ts`,
   `packages/sim-core/src/m1-invariants.test.ts`,
   `packages/benchmarks/src/cli-lib.ts` and
   `tools/headless-runner/src/index.ts`.
2. Run `pnpm test --filter m1-invariants`, `pnpm bench` and
   `pnpm sim:run -- --seed 1 --scenario hauling-building --ticks 100000`.
3. Check `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json` for
   benchmark environment metadata, invariant comparisons and the explicit
   `map-dirty` canonical hash value `0xba7253ca`.
