# WM-0143 Work report

## Outcome

Repaired the compact first-play start surface clipping exposed by WM-0140 by adding compact-only density styles for the first-play guidance panel. The default start surface still shows the player guidance, available actions, next goal, language controls, and diagnostics-free copy, but the 390x720 and large-UI compact paths are now viewport-bounded and scroll-reachable without lowering E2E thresholds.

## Files and public contracts changed

- `packages/ui-react/src/shell-main-menu-surface.ts`: passes the compact layout mode into first-play guidance rendering and uses compact-only panel/card/list spacing for the start-surface guidance stack.
- `coordination/tasks/WM-0143.json`: task claim metadata from `taskctl claim`.

## Acceptance evidence

- Compact 390x720 start surface remains viewport-bounded: focused E2E `keeps the main menu viewport-bounded and scroll-reachable at compact viewports` passed.
- Large UI scale compact start surface remains readable: focused E2E `keeps the shell readable when large UI scale is selected at compact viewports` passed.
- Default player HUD and diagnostics overlay were not regressed: full `web-shell` E2E passed 12/12, including start surface, player HUD, diagnostics, selection, camera, locale, and lamp action coverage.
- WM-0140 can resume without threshold changes: no E2E assertion was weakened; the fix reduces compact first-play guidance height from about 713px to about 605px in en/standard and from about 709.5px to about 597.3px in zh-CN/large at 390x720.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `corepack pnpm exec vitest run apps/web/src/web-shell.e2e.test.ts -t "keeps the main menu viewport-bounded"` | Pass | Reproduced the original failure before the fix, then passed after the compact style change. |
| `corepack pnpm exec vitest run apps/web/src/web-shell.e2e.test.ts -t "keeps the main menu viewport-bounded|keeps the shell readable when large UI scale"` | Pass | 1 file, 2 tests passed. |
| `node tools/test-runner.mjs e2e --filter web-shell` | Pass | 1 file, 12 tests passed in 316.36s. |
| `node tools/validate-handoff.mjs` | Pass | 1381 files, 143 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | 143 tasks, 10 roles. |
| `git diff --check` | Pass | No whitespace errors. |
| `corepack pnpm quality` | Pass | typecheck, lint, format, localization, content, boundaries, M7 checklist, and 71 unit files / 394 tests passed. |

## Determinism, save, performance and security impact

Determinism: unchanged; no simulation, Worker, random stream, clock, or authoritative state touched.

Save: unchanged; no save schema, storage key, or persistence behavior touched.

Performance: bounded React style selection only; no simulation hot path touched.

Security: unchanged; no new dependency, IPC, filesystem, telemetry, account, hosted service, or network behavior.

## Decisions and deviations

- Kept the change compact-only so desktop/medium first-play presentation remains visually unchanged.
- Did not shorten or remove first-play guidance copy; player guidance remains present and diagnostics-free.
- `node tools/test-runner.mjs e2e --filter web-shell` regenerated `coordination/reports/WM-0135-responsive-layout.md` as a side effect. That old report update is outside WM-0143 allowed paths, so the generated side effect was reverted before completion.

## Remaining risks / follow-up tasks

- WM-0140 must rerun the visual evidence gate after this repair is integrated.
- The start surface still uses provisional CSS-rendered UI art; final cut art remains future production work under the existing asset replacement contract.

## Handoff to reviewer

Inspect `packages/ui-react/src/shell-main-menu-surface.ts` and run `node tools/test-runner.mjs e2e --filter web-shell`. The highest-signal regression is the 390x720 compact start surface before and after the compact first-play guidance style path.
