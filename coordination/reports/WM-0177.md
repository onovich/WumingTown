# WM-0177 Work report

## Outcome

The additive caller-owned owner surfaces are implemented and the original
benchmark blocker is cleared without a waiver. The reviewed product
measurement commit `3ca83d19ed6b3146ac876d78dd8dc16e2d994d22` passed one
quiet-host admission and exactly six benchmark invocations against the
unchanged canonical baseline. The full run reports 15 OK, 0 WARN, 0 FAIL and
zero invariant mismatches. Same-host pairing with the repaired-main WM-0178
campaign shows zero invariant drift; the largest positive observed paired
median delta is `4.7327094761608945%` for `work-offers`. That observation is
not a gate or a newly introduced threshold.

The evidence is imported byte-for-byte under
`coordination/artifacts/WM-0177/pre-unblock-paired/` and bound by
`comparison-manifest.json` SHA-256
`45D9E35EC6FA462A9C0BA9E7E6A543BE097CEE0A7465AE446C498212802B4C11`.
No admission or benchmark was rerun during import, post-import validation or
the independent-review repair.

## Lineage and scope

- Original implementation checkpoint:
  `054d5017df3015faf63446929530a9d88c0b539d`.
- Repaired-main measurement merge:
  `3ca83d19ed6b3146ac876d78dd8dc16e2d994d22`.
- Post-unblock control commit:
  `a6addf904d2a2fc87a3608426d2b2439affbac4f`.
- Review-repair control base:
  `40b2cdee85f0405d949550bf2636148d36933c20`. The review-repair HEAD
  containing this report is intentionally distinct from measurement HEAD
  `3ca83d19ed6b3146ac876d78dd8dc16e2d994d22`; performance artifacts remain
  bound to the measurement HEAD rather than being relabeled as repair output.
- Repaired-main reference campaign commit:
  `dba8e88b4dc8936f95b050d0bb0e195063999bb2`, rooted at
  `coordination/artifacts/WM-0178/final-repaired-main`.
- The implementation remains exactly seven product/test/export files, below
  the unchanged cap of 12. The independently measured pre-repair blobs are:

| File | Blob OID at measurement `3ca83d` |
| --- | --- |
| `packages/sim-core/src/map-grid.ts` | `7c2a97746ecf1429e3bc51dbd2729435df77efe7` |
| `packages/sim-core/src/pathing.ts` | `5ccd68e749db287ac7aa899f8db0267a5b4f3e3f` |
| `packages/sim-core/src/work-offers.ts` | `f36dea966ec34767ce81c8fb4cb7b14fb54d8d85` |
| `packages/sim-core/src/reservation-ledger.ts` | `c5f59a5c59985dac2a1983413f72f693bef1f3ae` |
| `packages/sim-core/src/m3-health.ts` | `30ad6b0be0aadc6acbfa79154789cc4713afe0ea` |
| `packages/sim-core/src/index.ts` | `ffac8343a795dd6737797c52ac22ba5bcba45230` |
| `packages/sim-core/src/owner-hot-path-surfaces.test.ts` | `80a270805646c0480f4084ae68a878e40b426b80` |

The review repair changes six of those files: `pathing.ts` gains only four
failure-branch scalar writes; the five implementation modules gain only
module-local source-audit exports/comments with all 26 helper bodies byte-for-
byte AST-equal to `3ca83d`; and the existing owner-hot-path test gains the P1
regressions plus the P2 scanner. `packages/sim-core/src/index.ts` remains blob
`ffac8343a795dd6737797c52ac22ba5bcba45230`, so none of the audit-only helpers
is re-exported from the package root.

No GameSession/autonomy implementation, protocol, Worker, renderer, UI,
save/load schema, dependency, PR-3, content or release surface changed.

## Independent-review repair

Canvas requested two corrections in `MSG-MRHSN7GU-411FA6`:

1. **High — incomplete capacity-failure facts.** `writeFoundPathInto` knew the
   reconstructed count and goal cost but returned zeros when the route output
   was too small. The capacity-failure branch now publishes the known
   `pathCellCount` and `pathCostMilli` before setting
   `path_output_capacity_exceeded`; it does not write any route element. The
   start-equals-goal zero-capacity branch likewise reports count 1, cost 0 and
   node expansions 0. Regressions retain request/start/goal plus all five basis
   facts, exact legacy cost/node expansions and output identity, while proving
   the complete `[777]` route and zero-length route are byte-untouched. The
   sufficient-route success statements remain in their original order.
2. **Medium — incomplete allocation proof.** The old regex sampled 11 roots and
   38 class helpers but did not close transitive free/project-object calls or
   recognize several construction forms. It is replaced by one TypeScript
   scanner/AST matcher and a transitive call resolver over exactly 80 unique
   real-source labels: 11 Into roots + 42 class/public helpers + 27 free
   helpers. Cross-owner MapGrid, condition-store and EntityRegistry calls must
   resolve into that same audited set; an otherwise-valid hidden helper fails
   closure.

The AST matcher rejects `new`, object/array literals in assignment/return/
argument position, nested arrow/function/class declarations or expressions,
template/tagged/no-substitution literals, regex literals, Array/Object/String/
JSON factories, the reviewed allocation/string method calls, and syntactically
string-producing `+`/`+=`. Its positive regression matrix exercises every
category. Its negative matrix retains numeric `+`, `+= 1`, bitwise operations,
property writes, typed-index writes, control blocks and constant reason strings.

The native call allowlist is receiver-exact: `Number.isSafeInteger`,
`Math.abs/floor/max/min`, `fill` only on `claimIdOutput`, `this.seenEpoch` and
`this.closedEpoch`, and `get`/`set` only on the five reviewed ReservationLedger
Map receivers. `fake.get`, `fake.set` and `fake.fill` regressions must remain
unresolved. The only non-audited project-call sink is `failInternal` from
`ReservationLedger.allocateClaimId`: `acquireInto` has already proved capacity
before reaching it. That invariant-violation throw branch is explicitly not
part of the allocation-free claim; the same call from any other entry fails
closure.

Structural exception: `owner-hot-path-surfaces.test.ts` exceeds 400 lines
because Keystone explicitly required the TypeScript AST scanner to remain in
the existing test file, with no new file or path expansion. Most added lines
are the 80-entry audit table, positive/negative fixtures and parsing helpers;
production function size/complexity did not expand, and lint plus typecheck
cover the exception.

## Public API, allocation and non-benchmark evidence

- `WorkOfferIndex` stores and publishes owner, row and index versions through
  caller-owned mutation/read/selection outputs while retaining all legacy
  allocating wrappers and stable score/offer ordering.
- `MapGrid` and `GridPathfinder` use caller-owned movement/path outputs. The
  focused probe replaces the three legacy materializing map reads with throw
  stubs and still resolves the exact path; short route output publishes no
  partial path.
- `ReservationLedger.acquireInto` validates the complete caller-owned prepared
  transaction before one atomic commit. Every tested failure preserves ledger
  state/version and deterministically resets outputs.
- `M3AbilityCacheStore.queryAbilityInto` preserves hit, rebuild, stale, denied
  and invalid-actor semantics through a reusable result lane.
- The 80-label AST/closure audit covers every new Into root and complete
  transitive reviewed helper set. It found no reachable per-call object, array,
  typed-array, closure, regex or constructed-string creation; legacy allocating
  wrappers and the unreachable `failInternal` throw branch remain explicitly
  outside this proof.
- Before the one-shot performance campaign, the unchanged measurement product
  passed typecheck; the six public-root focused files passed 51 tests; M1, M2
  and M3 100000-tick runs passed with hashes `0xf7815189`, `0x6943dad4` and
  `0x7eb81a69`; boundaries passed for 16 workspaces; handoff and task control
  validation passed. The review-repair tree independently repeats the six-file
  focused command at 52 tests, the exact three 100000-tick hashes, typecheck,
  lint and 16-workspace boundaries. Final quality repeats the current type,
  unit, lint, format, localization, content and boundary gates.

## Quiet-host admission

The production admission tool was invoked exactly once for measurement commit
`3ca83d19ed6b3146ac876d78dd8dc16e2d994d22`; there were no retries. Its
stdout was imported byte-for-byte as `admission.json`:

- 1,351 bytes, exactly one CRLF-terminated JSON line;
- SHA-256
  `2FFC6B92812F9F5994D4EE4F0D3017DB82EE652F63D995A07E5219CCBF0FFCE9`;
- stderr: 0 bytes; status `pass`, reason `admission_passed`, exit code 0;
- two six-second windows: host busy `19.701096%` and `20.289602%`, maximum
  process host share `6.270003%` and `6.237956%`;
- fixed limits remained 40% host and 8% maximum process; ACShadows was absent,
  git was clean before/after, the commit was unchanged and no concurrent
  benchmark was present.

The import first staged the exact LF `.gitattributes` rule
`admission.json -text -diff`. `git check-attr text diff` reported both
attributes unset. After an ordinary `git add`, worktree raw OID and index OID
were both `bfb7d364b17bcd3b0bec49ce6fb1d6ba9881a895`; staged raw SHA-256 was
the expected `2FFC6B...FFCE9`. A second ordinary add preserved both values.
No renormalization or low-level index fallback was used.

## Once-only benchmark campaign

Every command used Node `v24.13.1`, pnpm `11.8.0`, Windows release
`10.0.26200`, 16 logical processors and host key
`55442F953FEA3046AA652002ACBA5F947F40C7B767FBE51A63885BCC49E95912`.
Each run used isolated Node-per-suite execution, 2 warmups and 9 measured
samples. The commands ran once each, in the order below, with no retry:

| Order | Command | Imported JSON SHA-256 | Results OK/WARN/FAIL | Invariant mismatches |
| ---: | --- | --- | ---: | ---: |
| 1 | `corepack pnpm bench --filter m3-ordinary-life-long-run` | `5998E66E40D34652E84B938066F08119E8694913CD3B6D34D40283221AB3AF81` | 1/0/0 | 0 |
| 2 | `corepack pnpm bench --filter m4-core-vertical-slice-long-run` | `1EEA8A772525795F22A9D5A1B6957D190624E5DC9CFD5A7A50DBAE15D0741EF9` | 1/0/0 | 0 |
| 3 | `corepack pnpm bench --filter m5-alpha-content-long-run` | `EA59A29D7DFB6C165B8EF3AB67A3B9F7469352757B2D85593C44BEBFE687C58D` | 1/0/0 | 0 |
| 4 | `corepack pnpm bench --filter map-dirty` | `DC36DFD0B6821116C8D0232C309B6EE724C871EF49C87FC45626F3F47C11DD38` | 1/0/0 | 0 |
| 5 | `corepack pnpm bench --filter spatial-index` | `43964D16847AB666C83AA376D097BCE755670583EB9B18277B8374CEC05901FF` | 1/0/0 | 0 |
| 6 | `corepack pnpm bench` | `E3BFEFD95EAA7BA56F0F832352BB245024688CD4BFEF3BB32A3D6B8E0D364679` | 15/0/0 | 0 |

The manifest additionally binds each imported sidecar file and canonical
payload SHA, plus the repaired-main reference JSON, reference sidecar and
reference canonical payload SHA. All 12 sidecar lines are exact and all 12
canonical payload hashes recompute from stored bytes.

## Same-host paired attribution

The formula for every row is exactly
`(task-reference)/reference*100`. The reference and task artifacts use the
same host and toolchain. Medians below are milliseconds; invariant equality is
deep equality of the stored invariant objects. Exact IEEE-754 values are in
the hashed manifest.

### Five filtered pairs

| Run | Suite | Repaired-main median | WM-0177 median | Delta | Status ref/task | Invariants |
| --- | --- | ---: | ---: | ---: | --- | --- |
| m3 | `m3-ordinary-life-long-run` | 17.817300 | 16.231200 | -8.902022% | OK/OK | equal |
| m4 | `m4-core-vertical-slice-long-run` | 4.707000 | 4.452900 | -5.398343% | OK/OK | equal |
| m5 | `m5-alpha-content-long-run` | 58.272800 | 49.040500 | -15.843241% | OK/OK | equal |
| map-dirty | `map-dirty` | 0.900100 | 0.917100 | +1.888679% | OK/OK | equal |
| spatial-index | `spatial-index` | 1475.201300 | 1437.944300 | -2.525554% | OK/OK | equal |

### Full-run 15 pairs

| Suite | Repaired-main median | WM-0177 median | Delta | Status ref/task | Invariants |
| --- | ---: | ---: | ---: | --- | --- |
| `empty-tick` | 5.926300 | 5.404700 | -8.801444% | OK/OK | equal |
| `entity-store` | 1.517000 | 1.287600 | -15.121951% | OK/OK | equal |
| `logistics-10k` | 10.788400 | 10.385100 | -3.738274% | OK/OK | equal |
| `m1-hauling-building-long-run` | 1.237700 | 1.181200 | -4.564919% | OK/OK | equal |
| `m2-pathing-invalidation` | 4.154700 | 4.014000 | -3.386526% | OK/OK | equal |
| `m2-work-logistics-long-run` | 2.855100 | 2.542200 | -10.959336% | OK/OK | equal |
| `m3-ordinary-life-long-run` | 16.928400 | 16.959200 | +0.181943% | OK/OK | equal |
| `m4-core-vertical-slice-long-run` | 4.073500 | 4.135200 | +1.514668% | OK/OK | equal |
| `m5-alpha-content-long-run` | 49.588100 | 45.427300 | -8.390723% | OK/OK | equal |
| `map-dirty` | 0.9212875 | 0.86736875 | -5.852543% | OK/OK | equal |
| `pathing-100` | 3.503400 | 3.456500 | -1.338700% | OK/OK | equal |
| `reservations` | 1.007000 | 0.839100 | -16.673287% | OK/OK | equal |
| `region-room` | 3.273700 | 3.194700 | -2.413172% | OK/OK | equal |
| `spatial-index` | 1476.845900 | 1441.285200 | -2.407882% | OK/OK | equal |
| `work-offers` | 0.929700 | 0.973700 | **+4.7327094761608945%** | OK/OK | equal |

The maximum positive paired observation is therefore the `work-offers` row at
exactly `4.7327094761608945%`. It remains below the unchanged canonical warning
policy, but it is evidence only and is not itself a threshold or waiver.

## Immutable anchors and byte/statistics audits

- Canonical baseline `packages/benchmarks/baseline.json` remains blob
  `f67c942269b41c023d236749a3332c4ae3bfbd17`, file SHA-256
  `CB1A50BC1EECFC4AC308722461F7B606F42DB38CB63336FDC7B4E58E9ACDAD15`,
  with 9 samples, 2 warmups and unchanged 10%/20% warning/failure policy.
- `coordination/artifacts/WM-0083` remains tree
  `deaf9de87fe5988ba7a0a0979f1724b055bac9fd`; historical result and sidecar
  blobs remain `e92caaf1ff187dcb5f128be5f61cd4162f144863` and
  `cddf88c33338e60037c8a2b509b9b3cc819e7595`.
- Admission plus all six JSON/sidecar sources equal their imported targets
  byte-for-byte. All 17 imported evidence files have identical worktree OID,
  index OID and raw staged SHA-256. Every sidecar line, artifact file hash and
  canonical payload hash recomputes exactly.
- The current shared `createBenchmarkStats` implementation parsed all 20
  imported task results: 180/180 samples are finite and all 100 stored fields
  (`sampleCount`, min, median, max and mean) match by `Object.is`.
- Current `compareBenchmarkToBaseline` recomputation reports 20 OK and zero
  invariant mismatches, with exact suite order, commands, measurement commit,
  9/2 counts and recorded comparison scalars.
- All 20 paired manifest rows recompute exactly from the six repaired-main
  references: zero deep invariant mismatches and exact maximum positive delta.
- The first attempt to launch this read-only shared-stat audit through
  `pnpm exec tsx` failed before evidence loading because this repository does
  not install a `tsx` executable. The audit then used the repository-supported
  `node --experimental-strip-types --import
  ./tools/register-ts-extension-loader.mjs --input-type=module -` runtime and
  passed. This was an audit harness startup error, not an evidence failure or a
  retry of admission/benchmark measurement.

## Final checks

| Command / audit | Result |
| --- | --- |
| source/target/sidecar/canonical/raw staged audit | PASS: 17 files, all exact |
| current shared-stat and baseline recomputation | PASS: 20 results, 180 finite samples, 100 `Object.is` fields, 20 OK, 0 mismatch |
| paired repaired-main hash/invariant/delta audit | PASS: 20 rows, 0 mismatch |
| baseline and WM-0083 immutability anchors | PASS |
| P1 branch-local executable diff audit | PASS: only count/cost writes in two capacity-failure branches; success statement order unchanged; route bytes untouched |
| P2 AST body/scope audit | PASS: 80 unique labels; 26 free helper bodies equal `3ca83d`; audit-only exports/comments; package-root index unchanged |
| six-file focused Vitest | PASS: 6 files / 52 tests |
| M1/M2/M3 100000-tick runs | PASS: `0xf7815189` / `0x6943dad4` / `0x7eb81a69` |
| `corepack pnpm typecheck` and scoped lint | PASS |
| `corepack pnpm boundaries:check` | PASS: 16 workspaces |
| `node tools/validate-handoff.mjs` | PASS: 1,767 files, 180 tasks, 10 roles |
| `taskctl validate` / `taskctl status` | PASS: 180 tasks / 10 roles; WM-0177 sole in progress, WM-0170 blocked, 0 unread messages |
| worktree, measurement and committed-range `git diff --check` | PASS |
| `corepack pnpm quality` | PASS: typecheck, lint, format, localization, content, 16-workspace boundaries, M7 checklist, 79 test files / 482 tests |

The repair intentionally reruns the required non-performance correctness and
determinism gates. It does not rerun quiet-host admission or any benchmark. The
single final quality invocation is also non-performance and covers the current
type/unit/lint/format/localization/content/boundary chain.

The main-line range also contains the historical taskctl-generated blocker
message `coordination/inbox/systems-architect/MSG-MRGEE5Y5-1D50EA.json` from
the original checkpoint. It is preserved byte-for-byte and was not introduced
or modified by this repair. The review-repair delta from control base `40b2cde`
is limited to the six authorized sim-core files plus this report; evidence and
manifest files remain untouched.

## Determinism, save, performance and security impact

- Determinism: fixed M1/M2/M3 hashes remain exact; all focused semantics,
  canonical benchmark invariants and paired repaired-main invariants match.
- Save: no save/load format, codec, migration or compatibility promise changed.
- Performance: the unchanged canonical full gate passes 15/0/0 with zero
  mismatches. One admission and six measurements were used, with zero retries,
  no workload/baseline/threshold modification and no waiver. Keystone
  authorized reuse for the review repair because executable changes are
  confined to previously failing output-capacity branches, successful measured
  statement paths are unchanged, and P2 changes only audit visibility/comments
  and tests. That authorization is a provenance-preserving evidence reuse
  decision, not a performance waiver; no performance command was rerun.
- Security/boundaries: no DOM, IPC, Electron, filesystem, network, untrusted
  data, runtime dependency or package-boundary surface changed.

## Handoff to reviewer

Independently recheck both Canvas findings. For P1, verify complete failure facts
for normal and start-equals-goal output-capacity failures, untouched route bytes
and unchanged sufficient-route statement order. For P2, verify the 80-label
11+42+27 closure, AST positive/negative matrix, receiver-exact native allowlist,
foreign helper regressions and sole `ReservationLedger.allocateClaimId`
`failInternal` sink exclusion. Confirm the audit-only functions are absent from
the package-root index and their bodies equal `3ca83d`.

Then verify the artifact tree, manifest SHA/sidecar, admission raw-index
protection, six task/reference raw-sidecar-canonical hash sets, current
shared-stat and baseline recomputation, 20 paired rows and immutable baseline/
WM-0083 anchors. Confirm that repair HEAD and measurement HEAD remain distinct,
that no performance command was rerun or waived, that
`4.7327094761608945%` is only the observed maximum paired delta, and that
WM-0170 stays blocked until WM-0177 is independently verified, integrated and
done.
