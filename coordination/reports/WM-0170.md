# WM-0170 Blocker Report

## Outcome

Blocked before product implementation. The mandatory public-API audit found
that the existing indexed selection, exact path, ability and atomic reservation
surfaces cannot satisfy WM-0170's owner-version revalidation and zero-allocation
hot-path contract within the task's allowed files.

No autonomy implementation, generic owner, GameSession integration, protocol,
Worker, Web, save, dependency, PR-3 or release file changed.

## Blocking evidence

| Public surface | Evidence | Missing contract |
| --- | --- | --- |
| `WorkOfferIndex` | `WorkOfferInput` has no owner-version field; the class exposes no index version; `readOffer` and selection return new objects | Version-bound selected row and allocation-free caller-owned read/selection output |
| `GridPathfinder` | `findPath` allocates a new `Uint32Array` on trivial and normal successful paths | Caller-owned bounded path output/result lanes |
| `ReservationLedger` | `acquire` builds `PreparedClaim[]` and `claimIds[]` for every transaction | Caller-owned prepared transaction and claim-id scratch while preserving one atomic commit |
| `M3AbilityCacheStore` | `queryAbility` returns a newly materialized result object | Caller-owned ability result lane for the hot arbitration path |

The ReservationLedger's existing validation/commit behavior is atomic, but its
public transaction surface still allocates. Supplying an unrelated scalar
"owner version" beside a WorkOffer would not bind that version to the selected
row and would make stale rejection caller-asserted rather than owner-proven.

## Required architecture decision

Create and review a prerequisite owner-surface repair that either:

1. adds a monotonic/version-bound WorkOffer read contract and caller-owned
   `select/read/findPath/acquire/queryAbility` output/scratch lanes; or
2. explicitly revises WM-0170's zero-allocation and offer-owner-version
   acceptance contract with measured justification.

WM-0170 should remain blocked and unimplemented until that prerequisite is
verified, integrated and done. Expanding WM-0170's allowed paths ad hoc would
violate the reviewed strict-serial DAG and generic owner ownership boundary.

## Checks performed

- `taskctl validate`: PASS, 176 tasks / 10 roles before claim.
- `taskctl status`: PASS; WM-0170 was the sole ready task before claim.
- Git/worktree audit: clean `main` at `866507b`, equal to `origin/main`, one
  worktree before the task branch was created.
- Public-source allocation/version audit: FAIL against WM-0170 acceptance for
  the exact reasons above.

Required implementation tests were not run because product implementation did
not start. Running unrelated gates cannot clear an architecture/API blocker.

## 2026-07-13 second stop-line checkpoint

WM-0177 was later verified, integrated and done, and commit `2ac4416` restored
WM-0170 to `in_progress`. The ordinary-owner audit then passed for
WorkOffer/path/reservation/ability and Phase A/B1 began. Independent Canvas
review found that the audit had not covered the allocation-free need-driven
food/rest/medical exact-index path required by architecture section 6.

### Current non-complete checkpoint

- `game-session-autonomy-types.ts`, `-reasons.ts` and `-store.ts` contain the
  numeric state/reason lanes and internal snapshot owner.
- Store repair now rejects raw numeric states, time rollback, policy swaps and
  job-version rollback for the same active job, state-incompatible reasons,
  uncleared terminal routes/claims, malformed typed-array snapshots and
  current/durable terminal divergence.
- Claiming persists an explicit `pendingJobId`; durable terminal evidence keeps
  the interruption policy and job version.
- `game-session-autonomy-selection.ts` is interface-only. There is no
  coordinator implementation and Canvas explicitly requested changes to the
  source discriminator, five-lane arbitration and mutable claim-plan scratch.
- Focused checkpoint gates passed after stabilization: sim-core typecheck,
  ESLint, Prettier and 9 autonomy tests. This is checkpoint evidence only, not
  task completion or independent verification.

### Independent architecture decision

The systems-architect selected strict prerequisite B and rejected mirroring
food/rest/treatment into generic WorkOffer rows. WorkOffer keys cannot preserve
meal-window, rest schedule/weather, patient-condition and caregiver-ability
bases without duplicating owner truth and creating non-atomic dual publication.

WM-0181 is therefore the sole repair writer for caller-owned, version-bound
Food/Rest/Medical `Into` selection and row-read surfaces. The DAG is now:

```text
WM-0177 -> WM-0181 -> WM-0170 -> WM-0171
```

WM-0170 is blocked again without lowering its acceptance. Its dirty Phase A/B1
checkpoint is retained and no coordinator body, completion, review or
integration action may run until WM-0181 is independently verified, integrated
and done and the exact resume procedure in the task packet passes.
