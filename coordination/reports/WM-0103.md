# WM-0103 Work Report

## Outcome

WM-0103 adds controlled external test instructions for the unsigned Windows
unpacked directory build. The instructions preserve the M6 Windows verdict and
make tester setup, run steps, support collection, known limitations and
non-goals explicit.

Changed:

- `docs/05_tech/09_m7_windows_controlled_external_test.md`
- `docs/05_tech/06_platform_matrix.md`
- `coordination/reports/WM-0103-plan.md`
- `coordination/reports/WM-0103.md`
- `coordination/tasks/WM-0103.json`

No Electron implementation, preload bridge, package config, signing, installer,
updater, store upload, public release, telemetry, account, crash upload, paid
service, public feedback system, save schema, public save compatibility or M8
work changed.

## Acceptance Evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Produces tester installation and run instructions for the unsigned unpacked Windows directory build | `docs/05_tech/09_m7_windows_controlled_external_test.md` includes producer and tester run instructions for `dist/desktop/win-unpacked/WumingTown.exe`. |
| Documents scope, warnings, diagnostics, support collection, sandbox/preload boundaries and known M6 limitations | The instructions include support collection, security boundaries, known limitations and non-goals. |
| States non-goals clearly: no signing, installer, updater, store upload, public release, telemetry, accounts, crash upload or paid services | The "Non-Goals" section lists these blocks explicitly. |
| Verifies current build and desktop-shell smoke commands still support the instructions | `pnpm build:desktop` and `pnpm test:e2e --filter desktop-shell` are required checks for this task. |
| Records Windows verdict as controlled external test only and does not broaden it | The document preserves `ready-for-controlled-external-test` and separates it from public release, signed installer and store readiness. |

## Checks Run

| Command | Result | Notes |
| --- | --- | --- |
| `pnpm typecheck` | Pass | TypeScript project check passed. |
| `pnpm build:desktop` | Pass | Produced unsigned unpacked directory; existing Vite chunk-size warning only. |
| `pnpm test:e2e --filter desktop-shell` | Pass | Used temporary `WM_ARTIFACT_DIR`; 1 file / 2 tests passed. Existing Node `DEP0190` and Vite chunk-size warnings only. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | 110 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | Pass | `in_progress: 1`, `ready: 4`, `proposed: 5`, `done: 100`, inbox `0`. |
| `git diff --check` | Pass | No whitespace errors. |
| `pnpm quality` | Pass | Typecheck, lint, format check, content validation, boundaries and 66 unit files / 353 tests passed. |

## Notes

WM-0103 intentionally does not add a desktop save bridge or host diagnostic
bridge. It carries forward the M6 blockers until reviewed narrow bridge tasks
exist.

## Handoff To Reviewer

Please verify:

- the Windows instructions match the unsigned unpacked directory verdict;
- tester support collection excludes private paths, secrets, broad logs,
  automatic crash upload and account/payment data;
- Electron security/preload boundaries remain stated and unchanged;
- no release, signing, store, telemetry, account, paid-service, save promise or
  M8 work started.
