# WM-0128 Work report

## Outcome

WM-0128 diagnoses the WM-0126 benchmark stop sign as measurement-only /
stale-execution evidence rather than a current product regression. The
previous failing WM-0126 benchmark artifacts record commit
`a0eaf0b66b0eeaeafeeed7844e6a9e12a7b11d3d`, while the current WM-0128 branch
and `origin/main` are at
`aa611cafc2e2060d689e077fa2cfdf9168bec46d`. The range from `a0eaf0b` to
`aa611ca` does not change `packages/sim-core` or `packages/benchmarks`, so
WM-0128 does not claim a hidden product repair. Instead, two fresh complete
`corepack pnpm bench` reruns on the current HEAD pass the unchanged 10 percent
warning / 20 percent blocking benchmark policy and clear the M8 benchmark stop
sign for closeout review.

## Files and public contracts changed

- `coordination/reports/WM-0128-plan.md`: execution plan created before any
  diagnosis or product edits.
- `coordination/reports/WM-0128.md`: this report and benchmark verdict.
- `coordination/tasks/WM-0128.json`: taskctl claim/completion state only.
- `coordination/thread-registry.json`: records the attempted
  `simulation-engineer` thread registration from the pre-restart dispatch.

No app, package, simulation runtime, Worker protocol, save schema, benchmark
baseline, benchmark threshold, Web/Windows platform verdict, release/store,
signing, telemetry, account, hosted-service, paid-service or public
save-compatibility surface is changed.

## Acceptance evidence

- `Diagnoses the repeated WM-0126 blocking benchmark regressions`
  - WM-0126 first artifact:
    `%TEMP%/wuming-town/WM-0126-bench/benchmarks/benchmark-results.json`.
    It records `gitCommit=a0eaf0b66b0eeaeafeeed7844e6a9e12a7b11d3d` and
    failures in `entity-store +48.09%`,
    `m4-core-vertical-slice-long-run +59.65%`, `map-dirty +50.34%` and
    `spatial-index +24.63%`.
  - WM-0126 confirmation artifact:
    `%TEMP%/wuming-town/WM-0126-bench-confirm/benchmarks/benchmark-results.json`.
    It also records `gitCommit=a0eaf0b66b0eeaeafeeed7844e6a9e12a7b11d3d` and
    repeated failures in `entity-store +41.31%`,
    `m4-core-vertical-slice-long-run +27.62%`, `map-dirty +27.73%` and
    `spatial-index +21.52%`.
  - WM-0128 current HEAD is
    `aa611cafc2e2060d689e077fa2cfdf9168bec46d`, matching `origin/main`.
    `git diff --name-only a0eaf0b..HEAD -- packages/sim-core packages/benchmarks`
    returns no files, proving no product or benchmark-source repair occurred
    between the failing artifact commit and this task's current code.

- `Repairs product or benchmark-harness regressions without weakening benchmark baselines, thresholds or protected M0-M7 facts`
  - No product or benchmark-harness code change was needed.
  - `packages/benchmarks/baseline.json`, `packages/benchmarks/src/cli-lib.ts`
    and `packages/benchmarks/src/baseline.ts` are unchanged in this branch.
  - The unchanged policy remains 10 percent warning and 20 percent blocking.

- `If a regression is environmental or measurement-only, records reproducible evidence and reviewer-verifiable rationale`
  - WM-0128 bench run 1 artifact:
    `%TEMP%/wuming-town/WM-0128-bench-1/benchmarks/benchmark-results.json`,
    canonical payload SHA-256
    `5674879F9C702E57761C5FF61F1A6D954005DB1FC2A83517387B09332A7FD879`.
    It records `gitCommit=aa611cafc2e2060d689e077fa2cfdf9168bec46d`,
    `sampleCount=9`, `warmupCount=2` and all comparisons passing.
  - WM-0128 bench run 2 artifact:
    `%TEMP%/wuming-town/WM-0128-bench-2/benchmarks/benchmark-results.json`,
    canonical payload SHA-256
    `FBBDC7CF103AE48D173B18EA7A412B6D9CA195747F9FD3901C28CDECBB909739`.
    It records the same current commit, sample count and warmup count, and all
    comparisons passing.
  - `corepack pnpm --version` reports `11.8.0`, matching
    `packageManager: pnpm@11.8.0`. The benchmark JSON metadata currently reads
    bare `pnpm --version`, which reports global `11.7.0`; that metadata caveat
    does not alter the recorded command output or benchmark comparison result.

- `Reruns corepack pnpm bench under the unchanged 10 percent warning / 20 percent blocking policy`
  - Run 1 target stop-sign values:
    `entity-store -0.03%`, `m4-core-vertical-slice-long-run -17.37%`,
    `map-dirty -18.76%`, `spatial-index +1.05%`.
  - Run 2 target stop-sign values:
    `entity-store -0.64%`, `m4-core-vertical-slice-long-run -13.04%`,
    `map-dirty -18.81%`, `spatial-index +0.76%`.
  - Both full-suite runs pass with no warnings and no failures. WM-0128
    therefore clears the benchmark stop sign for WM-0127 closeout review.

- `Does not change Web demo-only, Windows controlled external test, public release, store, signing, telemetry, account, paid-service or public save-compatibility owner gates`
  - No platform verdict or owner gate changed.
  - M8 closeout may proceed only as internal 1.0 readiness evidence and still
    must preserve Web `demo-only`, Windows unsigned controlled external test,
    public release, store, signing, telemetry, accounts, paid services and
    public save-compatibility as owner-gated decisions.

## Checks run

| Command | Result | Artifact / notes |
| --- | --- | --- |
| `corepack pnpm bench` | PASS | Run 1 with `WM_ARTIFACT_DIR=%TEMP%/wuming-town/WM-0128-bench-1`; all 15 comparisons passed. |
| `corepack pnpm bench` | PASS | Run 2 with `WM_ARTIFACT_DIR=%TEMP%/wuming-town/WM-0128-bench-2`; all 15 comparisons passed. |
| `corepack pnpm typecheck` | PASS | `tsc --noEmit --project tsconfig.typecheck.json --pretty false`. |
| `corepack pnpm test --filter m5-invariants` | PASS | 2 files / 3 tests passed. |
| `corepack pnpm sim:run -- --seed 5 --scenario m5-alpha-content-framework --ticks 100000` | PASS | Preserved M5 facts: seed `155`, content hash `0xe55d3015`, command stream hash `0x81d37435`, final world/read-model hashes `0xfba70a5c` / `0x9ba83cb7`. |
| `node tools/validate-handoff.mjs` | PASS | Final rerun after report updates: 1307 files, 128 tasks, 10 roles; structure digest omitted to avoid self-referential churn. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | PASS | Validation passed: 128 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | PASS | Snapshot before completion: `proposed: 1`, `in_progress: 1`, `done: 126`, unread inbox `0`. |
| `git diff --check` | PASS | No whitespace errors. |
| `corepack pnpm quality` | PASS | Typecheck, lint, format check, localization validation, content validation, boundary check, M7 checklist and 71 unit files / 387 tests passed. |

## Determinism, save, performance and security impact

- Determinism: no runtime determinism code changed. The M5 100000-tick scenario
  preserves the reviewed content, command stream, world and read-model hashes.
- Save: no save schema, save replay, platform save bridge, migration policy or
  public compatibility promise changed.
- Performance: no baseline or threshold changed. Two fresh current-HEAD full
  benchmark runs pass the unchanged policy and replace the stale stop sign with
  reviewer-verifiable current evidence.
- Security: no Electron preload, IPC, network, telemetry, account, signing,
  hosted-service, upload or paid-service surface changed.

## Decisions and deviations

- Chose explanation over code repair because the reproduced current-HEAD
  evidence passes twice and the failing WM-0126 artifacts do not correspond to
  the current HEAD used for WM-0128 review.
- Did not edit benchmark metadata even though the CLI records bare
  `pnpm --version` in JSON. `packages/benchmarks/**` is outside WM-0128's
  allowed path list, and the metadata mismatch is not the performance
  comparison itself. A future narrow harness task can align the JSON metadata
  with the actual `corepack pnpm` invocation if desired.

## Remaining risks / follow-up tasks

- Benchmark measurements remain machine-sensitive. The final WM-0127 M8
  closeout should cite the WM-0128 artifact paths and may rerun `bench` once
  more as part of its full gate, but WM-0128 no longer has a repeated current
  blocking failure.
- The benchmark artifact metadata helper's bare `pnpm` lookup can report the
  global pnpm version instead of the `corepack pnpm` version used to launch the
  task. This is a documentation/metadata accuracy risk, not a current M8
  benchmark threshold failure.

## Handoff to reviewer

Review:

- `coordination/reports/WM-0128-plan.md`
- `coordination/reports/WM-0128.md`
- `coordination/tasks/WM-0128.json`
- `%TEMP%/wuming-town/WM-0128-bench-1/benchmarks/benchmark-results.json`
- `%TEMP%/wuming-town/WM-0128-bench-2/benchmarks/benchmark-results.json`

Fast reproduction:

1. Confirm `git rev-parse HEAD` is
   `aa611cafc2e2060d689e077fa2cfdf9168bec46d`.
2. Run `corepack pnpm bench` with a fresh `WM_ARTIFACT_DIR`.
3. Confirm all comparisons pass and the four WM-0126 stop-sign benchmarks stay
   below the unchanged 20 percent blocking threshold.

Reviewer should verify that this task clears only the benchmark stop sign for
closeout review. It does not approve public release, public Web launch,
Windows public release, store submission, signing, telemetry, accounts, paid
services, final public claims or public save compatibility.
