# WM-0166 Work report — resumed PR-1 exit gates

## Outcome

PASS after the reviewed serial repair chain. The original WM-0166 conclusion is replaced by this fresh post-WM-0167/WM-0168 run; no stale result is reused.

- WM-0167 repaired the explicit GameSession job duration and bounded Worker wall-time debt while preserving the 100 ms / 3-tick quantum, deterministic core, protocol, schema and save boundaries.
- WM-0168 independently verified and integrated a real Chromium module Worker 600-second gate plus product-consumed default-Web moving/working evidence.
- This resumed WM-0166 branch was fast-forwarded to main `83c7892`, unblocked only after WM-0167 and WM-0168 were `done`, and reran every required check.

No product, protocol, schema, projection, save or dependency file is changed by the resumed owner work. Only this report and WM-0166 task control state change.

## Fresh real Worker and default Web evidence

The exact WM-0166 Worker smoke command passed 19/19. Its test-generated WM-0168 artifact was read before restoration and reported:

- browser-monotonic elapsed `600103.8 ms`, authoritative tick `0 -> 18000`;
- minute ticks `1800, 3603, 5403, 7200, 9003, 10800, 12600, 14403, 16203, 18000`;
- 6001 publications, maximum publication gap `125.5 ms` (hard limit 5000 ms);
- coherent lifecycle `idle@0 -> moving@3 -> moving@6 -> working@9 -> working@12 -> idle@15`;
- zero fatal messages, pre-cleanup closures, coherence failures and dropped snapshots; maximum reliable queue depth 2;
- browser session lifecycle `created -> initializing -> active`, with explicit cleanup separately observed as `destroyed`;
- exact input remained one `InitSession`; no UI advance, wait, drain, command-drain, repeated command loop or direct `runScheduledQuantum` drove time.

The generated `coordination/artifacts/WM-0168/real-worker-600s.json` drift was restored as required; the integrated reviewed WM-0168 artifact remains unchanged in repository history.

The exact Web command passed 13/13. At 1424x861 its Vite test-only observer records the production-assembled `WorldReadModel` only after `activeRenderer.setReadModel(nextReadModel)`, paired with the accepted GameSession basis. It proves product-consumed moving ticks 3/6 and later working ticks 9/12 rather than reading raw Worker messages. The same test proves eight residents, four resource lanes, 22 render rows, selection details, `projectionSource=game-session-worker`, schema-v3 negotiation, no gameplay command clock, and fixture/advance/wait/drain exclusion.

## PR-1 reused stores and deterministic surfaces

| Reused surface | Integrated PR-1 role |
|---|---|
| `EntityRegistry` | Stable `EntityId(index,generation)` for residents, resources, structures and contained stacks. |
| `MapGrid` | 64x64 authoritative map and navigation/version basis. |
| `LocationStore` | Authoritative map/container membership and positions. |
| `SpatialIndex` | Bounded direct spatial queries; no global pawn scan. |
| `ReservationLedger` | Sole active claim authority with explicit acquisition/release. |
| `JobCoreStore` | Serializable reserve/move/work/complete execution state and terminal history. |
| `WorkOfferIndex` | Exact-bucket bounded work discovery. |
| `ReasonTraceStore` | Structured idle/blocked/failure codes and parameters. |
| `GridPathfinder` | Bounded exact path solution. |
| `PathRequestBatcher` | Versioned path queue, result basis and stale rejection. |
| `ItemStackStore` | Food, wood, stone and lamp-oil owner quantities. |
| `StorageLogisticsIndex` | Storage supply/candidate indexing. |
| `BuildSiteStore` | PR-1 shelter material/work target. |
| `NeedStore` | Eight residents' authoritative need lanes. |
| `NeedUrgencyIndex` | Bounded need dirty refresh/candidate basis. |
| `RestSleepStore` | Resident rest/sleep ownership. |
| `RestCandidateIndex` | Versioned bed/rest candidates. |
| `M3EnvironmentStore` | Integer day/night/weather progression. |
| `M4LampNetworkStore` | Fueled lamp owner/version and bounded dirty projection. |
| `NamedRandomStreams` | Seeded named deterministic stream positions. |
| Canonical world/read-model hash surfaces | Stable owner-fact lineage across headless, Worker and Web projections. |

Also reused: fixed 30 TPS integer time, stable command queue/order, owner/index/path basis versions, bounded dirty queues, read-only projection builders and the package-root headless entry.

## Historical M1-M8 capabilities still not connected

- M1: the full hauling/building scenario runtime, catalog mode, focused command stream and save/replay envelope remain regression-only; PR-1 reuses foundational stores.
- M2: broader logistics, priority/candidate behavior, focused projections and save/replay remain regression-only.
- M3: health/injury/treatment, medical indexes, abilities, full eating/rest drivers, mood/thought, relationships/social depth and detailed weather/schedule behavior remain unconnected; PR-1 uses minimum needs/rest/day-night facts.
- M4: Chronicle/evidence, obligation, town rules, borrowed shadow, director pressure/crisis and the full lamp scenario/save remain unconnected; PR-1 uses one lamp and one build target.
- M5: anomaly roster/content, faction/governance precursors, alpha runner, focused projection and save/replay remain test-only.
- M6: static product-gate fixture, reviewed playback, OPFS evidence envelope, diagnostics/package gates and smoke models remain diagnostics/tests only, never GameSession authority or load input.
- M7: playtest checklist and historical usability evidence remain regression/documentation gates; telemetry, account, public feedback and release workflows remain unconnected.
- M8: seasons, old bridge, third knock, faction endgame, its runner and focused save/replay remain regression-only.
- Cross-milestone: legacy catalog scenario modes, `PlayableProjectionV1`, WM-0150 two-pawn slice and advance/wait/drain helpers remain tests/tools only. GameSession save/load remains unsupported and fail-closed; no public compatibility exists.

## Fresh required checks

| Command | Result |
|---|---|
| `corepack pnpm typecheck` | PASS |
| Required focused Vitest command | PASS, 4 files / 44 tests |
| `node tools/test-runner.mjs e2e --filter worker-smoke` | PASS, 19/19; real 600-second evidence above |
| `node tools/test-runner.mjs e2e --filter web-shell` | PASS, 13/13 |
| `corepack pnpm sim:run -- --seed 5 --scenario pr1-integrated-gamesession --ticks 18000` | PASS; world `0x695bd812`, read `0x84ecbaa1`, zero leak/backlog/resource delta |
| `corepack pnpm sim:run -- --seed 5 --scenario pr1-integrated-gamesession --ticks 100000` | PASS; world `0xa268d08d`, read `0xaaa21e3e`, zero leak/backlog/resource delta |
| `corepack pnpm boundaries:check` | PASS, 16 workspaces |
| `node tools/validate-handoff.mjs` | PASS, 168 tasks / 10 roles |
| `taskctl validate` / `taskctl status` | PASS; WM-0166 is the sole in-progress task during evidence |
| `git diff --check` | PASS |
| `corepack pnpm quality` | PASS; typecheck/lint/format/localization/content/boundaries/checklist and 78 files / 462 tests |

Web E2E regenerated `coordination/reports/WM-0135-responsive-layout.md`; Worker smoke regenerated `coordination/artifacts/WM-0168/real-worker-600s.json`. Both out-of-scope diffs were inspected and restored.

## Acceptance closure

- Ten-minute normal-speed Worker clock: closed by the fresh 600103.8 ms real Chromium run, not headless substitution.
- Same-session town facts: coherent basis validation covers residents, resources, alerts, jobs and details; default Web consumes the matching accepted frame.
- Worker/headless parity and schema-v3 fail closure: focused 44/44 pass.
- 100000-tick conservation: zero jobs, reservations, commands, paths, dirty queues and resource delta; structured divergence tests retain tick/owner/reason evidence.
- Fixture retirement: default module/semantic guards exclude `WEB_PRODUCT_GATE_READ_MODEL`, reviewed playback and advance/wait/drain authority.
- Visible non-instant work: moving and working each span two consecutive coherent publications and are consumed by the Pixi read-model path before terminal idle.
- Structured reasons: idle/blocked/failed and protocol/lifecycle errors remain coded and parameterized.
- M1-M8 regression, boundary, security and quality gates pass; no reviewed exception is required.

## Residual risks and boundaries

- Real-time evidence is from local Chromium on this Windows host. Other OS/background-tab suspension policies remain platform variability; the measured maximum gap is far below budget.
- Hard Worker process-crash observation beyond the public browser-session lifecycle still requires a separately reviewed error hook.
- Projection v1 retains generic job markers and no terrain/job-kind lane; Web does not synthesize missing authority.
- PR-1 intentionally integrates a minimum bounded town slice, not the unconnected historical capabilities listed above.
- GameSession save/load is unsupported and fail-closed. No public save, release, telemetry, account or PR-2+ promise is introduced.
- ADR status closure and any next-phase planning belong to systems-architect/project-director after independent WM-0166 review and integration.

## Reviewer handoff

Review this report as a replacement for the original stale pass conclusion. Re-run every required check, inspect the 19-test Worker artifact generation and 13-test Web product-consumed observer, and confirm the owner diff contains no production/protocol/schema/save change. This owner has not reviewed, integrated, merged, pushed or marked WM-0166 done.
