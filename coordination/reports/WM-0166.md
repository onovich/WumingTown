# WM-0166 Work report

## Outcome

PR-1 integrated GameSession exit gates pass without product, protocol, schema, save, or threshold changes. The default Web route advances Worker-owned GameSession time without UI advance/drain helpers, presents at least eight residents plus authoritative resources and selection details from one coherent projection basis, and remains mechanically isolated from static gameplay fixtures. Worker/headless hashes match for the fixed seed and command stream. The 18000-tick and 100000-tick runs finish with zero active jobs, reservations, command/path queues, derived backlogs, or resource conservation delta.

WM-0166 changed only coordination plan/report/task state. Existing tests already supplied the required hard gates, so no test threshold or production implementation was modified.

## Gate evidence

- Continuous normal speed: `worker-smoke.e2e.test.ts` advances negotiated GameSession projections in a real browser module Worker without UI helpers. `web-shell.e2e.test.ts` observes advancing default-route truth without advance, drain, or canvas-dispatch helpers. The fixed 30 TPS 18000-tick run is the deterministic ten-minute simulation horizon.
- One-session lineage: focused Web adapter tests require a coherent render/UI pair with matching session, scenario, tick, snapshot sequence, world hash, read-model hash, content hash, map basis, owner versions, eight residents, resources, jobs and selection detail. Web E2E verifies the default route reports `projectionSource=game-session-worker` and selects resident/resource/structure details.
- Worker/headless parity: fixed seed `5` plus `parity.noop` reaches tick 30 with matching Worker/headless world and read-model hashes. Initial and subsequent projection pairs are coherence-checked.
- Long-run conservation: tick 100000 yields world `0x49765161`, read model `0xcc82ca6b`, 334/334 accepted paths, 6853 node expansions, lifecycle peaks 1 job/1 reservation, terminal job 1, reservation acquire/release 1/1, and zero terminal leaks/backlogs/resource delta.
- First divergence diagnostics: forced food divergence reports `game_session.resource_conservation_failed` with tick, resource def id, expected and actual. Job/reservation diagnostics similarly carry structured owner/reason fields; unsupported/malformed projection and lifecycle failures fail closed with structured reason codes.
- Non-instant visibility: GameSession projections publish the explicit JobCore lifecycle across movement/working publications; the default Web E2E requires advancing ticks and observable resident movement rather than instant drain-driven completion.
- Schema v3: protocol tests cover exact projection v1 negotiation and validators; Worker/Web tests cover exact Ready acceptance, missing negotiation, malformed/incoherent projection, wrong/newer contracts and fatal closure without legacy/static fallback.
- Fixture retirement: the semantic/default-graph Web test rejects imports or gameplay reads from `WEB_PRODUCT_GATE_READ_MODEL`, `reviewed-playable-session`, WM-0150 advance/wait/drain helpers, source internals, and diagnostic branches. The 13 Web E2E tests pass on the default Worker projection route.

## PR-1 reused stores and deterministic surfaces

Every owner store/surface reused by the integrated runtime:

| Reused owner/index surface | PR-1 role |
|---|---|
| `EntityRegistry` | Stable `EntityId(index,generation)` for residents, resources, structures and contained stacks. |
| `MapGrid` | 64x64 authoritative map and navigation/version basis. |
| `LocationStore` | Authoritative map/container membership and positions. |
| `SpatialIndex` | Direct bounded spatial queries; no global pawn scan. |
| `ReservationLedger` | Sole active claim authority, acquired/released by the explicit job lifecycle. |
| `JobCoreStore` | Serializable `reserve -> interact -> complete` execution state and terminal history. |
| `WorkOfferIndex` | Exact-bucket bounded work discovery. |
| `ReasonTraceStore` | Structured idle/blocked/failure reasons and scalar parameters. |
| `GridPathfinder` | Exact bounded path solution. |
| `PathRequestBatcher` | Versioned bounded path request/result queue and stale rejection. |
| `ItemStackStore` | Authoritative food, wood, stone and lamp-oil quantities. |
| `StorageLogisticsIndex` | Storage supply/candidate indexing. |
| `BuildSiteStore` | PR-1 shelter material/work target. |
| `NeedStore` | Eight residents' authoritative need lanes. |
| `NeedUrgencyIndex` | Bounded need dirty refresh/candidate basis. |
| `RestSleepStore` | Resident rest/sleep ownership. |
| `RestCandidateIndex` | Versioned bed/rest candidates. |
| `M3EnvironmentStore` | Integer day/night/weather progression. |
| `M4LampNetworkStore` | Fueled lamp owner/version and bounded dirty projection. |
| `NamedRandomStreams` | Seeded named deterministic stream positions. |
| Canonical world/read-model hash surfaces | Stable owner-fact lineage for headless/Worker/projection parity. |

The integrated runtime also reuses the fixed `TICKS_PER_SECOND=30` time basis, stable command queue/order, owner/index/path basis versions, bounded dirty queues, read-only projection builders and headless runner package-root entry.

## Historical M1-M8 capability inventory not connected to PR-1 product runtime

- M1: full hauling/building scenario runner, its focused Worker catalog mode, focused save/replay envelope and richer hauling/build command stream remain regression/test-only. PR-1 reuses foundational item/storage/job/reservation/build stores, not the M1 runtime.
- M2: broader storage logistics, work-priority/candidate behaviors, M2 scenario runner, focused projections and save/replay remain regression/test-only. PR-1 uses a bounded subset of storage/work/path surfaces.
- M3: health, injuries, treatment/medical indexes, abilities, complete eating/rest drivers, mood/thought, relationships/social simulation, detailed weather/schedule behavior and M3 save/replay remain unconnected. PR-1 connects minimum resident needs, rest fixtures and day/night basis only.
- M4: Chronicle/evidence, obligation, town rules, borrowed shadow, director pressure/crisis systems and full lamp scenario/save-replay remain unconnected. PR-1 connects one lamp owner and a build target only.
- M5: anomaly roster, anomaly content, factions/governance precursors, alpha scenario runner, projections and save/replay remain test-only.
- M6: historical static product-gate fixture, reviewed playback, OPFS evidence envelope, diagnostics/package gates and smoke read models remain diagnostics/tests only; they are not GameSession authority or load input.
- M7: playtest checklist and historical usability evidence remain regression/documentation gates; no telemetry, account, public feedback or release workflow is connected.
- M8: seasons, old bridge, third knock, faction endgame, associated scenario runner and focused save/replay remain regression/test-only.
- Cross-milestone: historical `catalogVersion` scenario modes, `PlayableProjectionV1`, WM-0150 two-pawn command slice and public advance/wait/drain helpers remain tests/tools only. GameSession save/load remains deliberately unsupported/fail-closed; no public compatibility exists.

## Required checks

| Command | Result | Evidence |
|---|---|---|
| `corepack pnpm typecheck` | PASS | Strict workspace typecheck. |
| Required focused Vitest command | PASS | 4 files, 40 tests. |
| `node tools/test-runner.mjs e2e --filter worker-smoke` | PASS | 1 file, 18 tests, including real browser Worker GameSession advance. |
| `node tools/test-runner.mjs e2e --filter web-shell` | PASS | 1 file, all 13 E2E tests. |
| `corepack pnpm sim:run -- --seed 5 --scenario pr1-integrated-gamesession --ticks 18000` | PASS | Tick 18000; world `0xe877a4aa`; read model `0x44305a8c`; zero leaks/backlogs/delta. |
| `corepack pnpm sim:run -- --seed 5 --scenario pr1-integrated-gamesession --ticks 100000` | PASS | Tick 100000; world `0x49765161`; read model `0xcc82ca6b`; zero leaks/backlogs/delta. |
| `corepack pnpm boundaries:check` | PASS | 16 workspaces. |
| `node tools/validate-handoff.mjs` | PASS | 1606 files, 166 tasks, 10 roles. |
| `taskctl validate` | PASS | 166 tasks, 10 roles. |
| `taskctl status` | PASS | WM-0166 sole in-progress task; no unread inbox messages. |
| `git diff --check` | PASS | No whitespace errors before report. |
| `corepack pnpm quality` | PASS | Typecheck, lint, format, localization, content, boundaries, checklist; 78 test files / 458 tests. |

Web E2E regenerated `coordination/reports/WM-0135-responsive-layout.md`; the generated out-of-scope drift was inspected and restored.

## Residual risks and reviewed boundaries

- The automated ten-minute gate proves the exact 18000-tick simulation horizon and real Worker-owned scheduler behavior separately; it does not hold a browser open for 600 wall-clock seconds. Host timer throttling/background-tab behavior remains a platform-level residual risk.
- Worker hard process-crash observation still lacks a reviewed public Worker error lifecycle hook.
- GameSession projection v1 has generic job markers and no job-kind or terrain lane; Web does not synthesize missing authority.
- The integrated PR-1 scenario intentionally exercises a minimal bounded lifecycle, not the full historical M1-M8 feature inventory listed above.
- GameSession save/load is unsupported and fails closed. Focused historical saves do not establish public compatibility.
- ADR-0017 remains marked Proposed even though its strictly serial implementation tasks have been executed; architecture status closure belongs to systems-architect/project-director, not QA.
- No release, store, signing, telemetry, account, paid service, public feedback, or public save promise was added.

## Reviewer handoff

Review the report against WM-0166 acceptance, rerun the required commands, and inspect the existing focused tests for continuous Worker scheduling, coherent projection basis, fixed-stream parity, terminal conservation diagnostics, schema-v3 fail closure and semantic default-route fixture exclusion. This owner has not reviewed, integrated, merged, pushed or marked the task done.
