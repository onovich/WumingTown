# WM-0139 Work report

## Outcome

Updated first-play guidance so new players see selectable targets, camera movement, and the minimum selectable-target -> `Prioritize lamp work` -> queued HUD feedback chain in both en and zh-CN, without adding simulation authority or public/final release claims.

## Files and public contracts changed

- `docs/01_design/10_m8_first_play_guidance.md`: clarified the first-play UI contract for selectable targets, camera movement, the WM-0138 lamp-priority local action chain, and post-dismiss reachability.
- `docs/04_content_balance/07_localization_text_templates.md`: added the provisional WM-0139 localization template contract and copy boundaries.
- `packages/ui-react/src/localization.ts`: added en/zh-CN player-facing first-play control and command-chain copy; removed diagnostic/debug wording from the main-menu guidance boundary.
- `packages/ui-react/src/shell-main-menu-surface.ts`: added selection, camera, and lamp-command guidance to the first-play start surface.
- `packages/ui-react/src/shell-hud.ts`: added a persistent player HUD first-play guidance card on desktop, medium, and compact layouts.
- `packages/ui-react/src/shell-hud.test.ts`: covered start-surface and HUD guidance in en/zh-CN, including queued zh-CN lamp-priority feedback.
- `coordination/tasks/WM-0139.json`: updated by `taskctl claim`.

## Acceptance evidence

- Default first-play guidance explains selection, camera movement and the minimum playable command without exposing diagnostics: `packages/ui-react/src/shell-hud.test.ts` asserts the main-menu guidance includes selectable target types, drag-pan/camera guidance, `Prioritize lamp work`, and does not contain diagnostic/debug wording.
- Guidance is localized in zh-CN and en and remains reachable through a player surface: `packages/ui-react/src/shell-hud.ts` renders `player-first-play-guidance` in the player HUD after the start surface is dismissed; `shell-hud.test.ts` asserts the card in both locales.
- Copy stays within controlled-test/demo boundaries and makes no public release or final balance claims: new copy only references visible player controls, internal-tool separation, and the shell-local queued HUD feedback.
- Guidance points to visible read-model state: selection copy names residents, structures, lantern posts, visitors and map tiles, and points to inspector/read-model state rather than hidden lore.
- Tests show onboarding in both locales after the command chain lands: `shell-hud.test.ts` covers en queued local lamp action and adds zh-CN first-play guidance alongside queued `wm0138.local_adapter.lamp_priority` feedback.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `corepack pnpm exec vitest run --exclude=**/*.e2e.test.ts packages/ui-react/src/shell-hud.test.ts packages/ui-react/src/localization.test.ts` | Pass | 2 files, 17 tests passed. |
| `corepack pnpm localization:validate` | Pass | zh-CN/en player-visible catalogs complete. |
| `corepack pnpm test --filter web-shell` | Unsupported | Repo unit runner reports `Unsupported unit test filter: web-shell`; command exits 1. |
| `node tools/validate-handoff.mjs` | Pass | 1376 files, 142 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | 142 tasks, 10 roles. |
| `git diff --check` | Pass | No whitespace errors. |
| `corepack pnpm quality` | Pass | typecheck, lint, format, localization, content, boundaries, M7 checklist, and 71 unit files / 394 tests passed. |

## Determinism, save, performance and security impact

Determinism: unchanged; no sim-core, Worker protocol, random stream, clock, or authoritative world state touched.

Save: unchanged; no save schema or persistence behavior changed.

Performance: bounded React HUD/menu rendering copy only; no simulation hot path touched.

Security: no new dependency, IPC, filesystem access, telemetry, account, or network behavior.

## Decisions and deviations

- Kept the change in `packages/ui-react/src/**` and docs only; did not touch `apps/web` or simulation packages.
- Used a persistent HUD guidance card rather than a reopen button because acceptance allows guidance to remain reachable through a player surface.
- Replaced diagnostic/debug wording in the default first-play boundary with internal-tool separation to satisfy the no-diagnostics copy requirement.
- `corepack pnpm test --filter web-shell` remains unsupported by the repo test runner; focused UI tests and `corepack pnpm quality` provide the automated evidence for this scoped UI package change.

## Remaining risks / follow-up tasks

- The lamp-priority action remains the WM-0138 shell-local adapter path, not authoritative Simulation Worker command handling.
- Chronicle and inspect command slots remain placeholders by prior scope.

## Handoff to reviewer

Fast path: inspect `packages/ui-react/src/localization.ts`, `packages/ui-react/src/shell-main-menu-surface.ts`, `packages/ui-react/src/shell-hud.ts`, and `packages/ui-react/src/shell-hud.test.ts`; then run the focused Vitest command and `corepack pnpm quality`.
