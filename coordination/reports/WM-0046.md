# WM-0046 Work report

## Outcome
Defined the executable M3 ordinary-life scenario contract
`m3.ordinary_life.injured_caregiver.v1` as the pre-architecture gate for M3
ordinary life simulation. The contract specifies deterministic seeds, tick
horizons, bounded actors/fixtures, a staged command stream, expected structured
reasons, invariants, save/replay checkpoints, Worker/headless parity
expectations, benchmark dimensions, non-goals, and explicit M4 exclusions.

## Files and public contracts changed
- `docs/02_systems/17_m3_ordinary_life_scenario.md`: new M3 scenario contract
  for downstream architecture and implementation tasks.
- `docs/07_roadmap/08_m3_ordinary_life_simulation_plan.md`: narrow pointer from
  WM-0046 to the contract path and scenario id.
- `coordination/tasks/WM-0046.json`: updated by `taskctl claim`; will be
  updated by `taskctl complete` after checks pass.
- `coordination/reports/WM-0046.md`: this work report.

## Acceptance evidence
- Focused M3 fixture with id, seed, tick horizons, command stream, actor bounds,
  needs, rest, food, injury, illness, medical care, abilities, mood,
  relationships, day/night, and weather basics: documented in
  `docs/02_systems/17_m3_ordinary_life_scenario.md`.
- Injury affects work, medical care logistics, mood, and at least one
  relationship with structured reasons: documented under Required Behavior and
  Expected Structured Reasons in the scenario contract.
- Fixture remains small and excludes M4 lamp, Chronicle, anomaly, crisis,
  director, broad content, and platform save UI work: documented under Fixture
  Bounds, Non-Goals, and Explicit M4 Exclusions.
- Invariants, benchmark dimensions, save/replay checkpoints, and
  Worker/headless parity expectations: documented under Invariants, Save,
  Replay, And Worker Parity, and Benchmark Dimensions.
- No forbidden implementation, package, app, tool, schema, protocol, save
  format, dependency, test, benchmark, or runtime file was edited: confirmed by
  `git status --short` and the final diff scope.

## Checks run
| Command | Result | Artifact / notes |
|---|---|---|
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | Pre-edit validation: 60 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | Pass | Pre-edit status: WM-0046 was the only ready task. |
| `node tools/validate-handoff.mjs` | Pass | Handoff validation passed: 649 files, 60 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | Validation passed: 60 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | Pass | WM-0046 in progress; unread inbox messages: 0. |
| `git diff --check` | Pass | No whitespace errors. |
| `pnpm quality` | Pass | Typecheck, lint, format check, content validation, boundary check, and 133 unit tests passed. |

## Determinism, save, performance and security impact
Determinism impact is specification-only: the new contract requires fixed 30
TPS, named seeds, stable ordering, seeded streams, integer lanes, bounded
indexed thinking, explicit serializable jobs, and checkpoint hash parity.
Save/replay impact is specification-only: the contract defines focused M3
checkpoint and resume expectations without approving public save compatibility
or migration. Performance impact is specification-only: benchmark dimensions
and caps are named, but no baseline is created. Security impact is none beyond
preserving existing authority boundaries; no runtime code, dependencies,
protocols, schemas, Worker APIs, Electron settings, or platform save paths were
changed.

## Decisions and deviations
No ADR was created. No deviation from task scope. The M3 contract uses
provisional test values and explicitly marks them as not final balance.

## Remaining risks / follow-up tasks
Downstream `WM-0047` must turn this contract into an approved architecture gate
before implementation. The exact reason enum names and owner-store boundaries
remain provisional until that ADR. Implementation tasks must block if they need
public save/protocol/schema changes, new dependencies, M4 systems, or broad
content work.

## Handoff to reviewer
Review the contract at `docs/02_systems/17_m3_ordinary_life_scenario.md`, then
confirm the roadmap pointer and this report. The fastest scope check is:
`git diff --stat` and `git diff --name-only`.
