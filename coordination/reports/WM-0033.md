# WM-0033 Work report

## Outcome

Defined the executable M2 work/logistics scenario contract as `m2.work_logistics.lantern_yard.v1`. The contract specifies a bounded 40 x 24 yard with 20 actors hauling/building, source stacks, depot storage, four lantern boundary build sites, a deterministic command stream, 100 versioned path requests under gate invalidation, tick horizons, terminal state, failure variants, invariants, reason trace expectations, save/replay and Worker/headless parity requirements. No product/runtime code, implementation task or M3 work was started.

## Execution plan

- Read required workflow, task, roadmap, system, testing and planning documents.
- Claim WM-0033 as `gameplay-designer`.
- Draft only the scenario/spec files allowed by the task.
- Map every acceptance item to concrete sections in the scenario contract.
- Run the required checks, then complete through `taskctl` to the reviewer.

## Files and public contracts changed

- `docs/02_systems/16_m2_work_logistics_scenario.md`: new scenario contract for `m2.work_logistics.lantern_yard.v1`.
- `docs/07_roadmap/07_m2_work_logistics_vertical_slice_plan.md`: links WM-0033 to the new scenario contract id/path.
- `coordination/tasks/WM-0033.json`: taskctl claim/complete metadata only.
- `coordination/reports/WM-0033.md`: this report.

## Acceptance evidence

| Acceptance criterion | Evidence |
| --- | --- |
| Scenario contract defines map bounds, actors, item sources, storage destinations, build/order targets, command stream, seeds, tick horizons and expected terminal state | `docs/02_systems/16_m2_work_logistics_scenario.md` sections `Fixture Bounds`, `Initial Entities`, `Command Stream And Horizons`, and `Expected Terminal State At Tick 20000`. |
| Scenario explicitly targets 20 actors hauling/building plus 100 path requests under versioned invalidation | Actor table defines `actor.worker.00` through `actor.worker.19`; command stream submits four path probe batches totaling exactly 100 requests and gate toggles require version-basis stale rejection. |
| Invariants cover reservation leaks, stale work offers, negative resources, queue growth, material conservation, hash divergence and structured reason traces | Contract `Invariants` section covers material conservation, reservation, stale work offers/indexes, bounded work selection, pathing/invalidation, queue growth, hash/replay/parity, and ReasonTrace. |
| Contract preserves Worker/headless authority, sim-core purity, indexed bounded work selection, serializable jobs, save/replay and Worker parity expectations | Contract `Authority And Implementation Guardrails` and `Hash, replay and parity` subsections state those boundaries directly. |
| Contract explicitly excludes broad economy, town-life, anomaly, combat, platform save UI, content expansion, balance production and M3 | Contract `Purpose And Non-Goals` lists the exclusions; roadmap remains inside the existing M2 DAG. |

## Checks run

| Command | Result | Artifact / notes |
| --- | --- | --- |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | `Validation passed: 43 task(s), 10 role(s).` |
| `node tools/validate-handoff.mjs` | Pass | `Handoff validation passed: 559 files, 43 tasks, 10 roles.` |
| `git diff --check` | Pass | No whitespace errors. |
| `pnpm quality` | Pass | Typecheck, lint, format check, content validation, boundary check and 107 unit tests passed. |

## Determinism, save, performance and security impact

- Determinism: no runtime behavior changed. The contract requires fixed 30 TPS, seeds, stable command stream, versioned path basis, replay diagnostics and hash equality.
- Save: no save format changed. The contract requires a focused midrun save/load/resume check and derived-index rebuild before resume.
- Performance: no benchmark baseline changed. The contract defines provisional caps for indexed offer visits, scoring, Top-K exact pathing, ReasonTrace storage, and 100 path request evidence.
- Security: no Electron, preload, filesystem, dependency or UI authority change. The contract preserves Worker/headless authority and read-only UI projections.

## Decisions and deviations

- No ADR was created or modified; WM-0034 remains the architecture gate before implementation.
- A separate `coordination/reports/WM-0033-plan.md` was not created because it is not in WM-0033 allowed paths. The execution plan is embedded in this report.
- Provisional canon and balance values are marked in the scenario contract. No final world hash is fixed before implementation evidence exists.

## Remaining risks / follow-up tasks

- WM-0034 must still record architecture boundaries before implementation tasks can run.
- WM-0035 through WM-0042 must implement and prove the scenario; this task only defines the executable contract.
- The final hash, benchmark baselines and artifact paths are intentionally left for implementation/evidence tasks after the scenario runner exists.

## Handoff to reviewer

Review `docs/02_systems/16_m2_work_logistics_scenario.md`, the one-line roadmap link, this report and the diff. Confirm the contract is executable, bounded, testable, scope-limited to M2, and preserves the project authority and determinism rules before approving downstream WM-0034.
