# WM-0007 Work report

## Outcome
Implemented generation-checked entity handles, bounded `Int32` component storage, stable ascending iteration, and a fixed-capacity structural command buffer with explicit commit. The independent reviewer `changes_requested` findings were addressed: public component reads now validate against the live `EntityRegistry`, queued Int32 command values are rejected before typed-array storage, and hot iteration/commit APIs now use primitive callbacks and reusable typed result buffers. After project-director owner review, `taskctl complete` was run again to request reviewer re-verification.

## Files and public contracts changed
- `packages/sim-core/src/entity-id.ts`: added `EntityId`, `EntityRegistry`, allocation, destruction, generation validation, capacity handling, and primitive stable alive iteration.
- `packages/sim-core/src/component-store.ts`: added bounded `Int32ComponentStore` with active bitmap, registry-backed generation validation, explicit capacity, attach/detach/set/read operations, and primitive stable ascending iteration.
- `packages/sim-core/src/structural-commands.ts`: added fixed-capacity structural command buffer and explicit commit phase for allocate, destroy, attach, detach, and set commands; commit writes into reusable typed result buffers.
- `packages/sim-core/src/structural-command-types.ts`: holds structural command constants, public result view definitions, reusable report helpers, and ordering helpers used internally by the command buffer.
- `packages/sim-core/src/index.ts`: exported the public entity/store/command APIs plus reusable structural command result view helpers.
- `packages/sim-core/src/entity-store.invariants.test.ts`: added invariant and regression tests for stale handles, reuse, deferred commits, stable ordering, conflicts, Int32 queue validation, typed result buffers, and capacity boundaries.
- `packages/benchmarks/src/index.ts`: added `entity-store` benchmark target exercising registry/store/buffer allocation, mutation, commit, primitive iteration, and reusable commit result buffers.
- `tools/test-runner.mjs`: added literal `--filter entity-store` support.
- `coordination/reports/WM-0007-plan.md`: recorded execution plan and verification plan.
- `coordination/tasks/WM-0007.json`, `coordination/thread-registry.json`: updated by `taskctl claim`.

## Acceptance evidence
- Destroyed entity handles cannot alias newly allocated entities: covered by `prevents destroyed handles from aliasing reused slots`; same slot is reused with incremented generation and stale handle fails validation.
- Iteration and conflict resolution use documented stable ordering: covered by ascending entity/component iteration test and conflict-order test. Command order is destroy, detach, attach, set, allocate; ties use entity index then insertion sequence.
- Structural changes are deferred to explicit commit phases: covered by test that queued attach/destroy has no effect until `commit`.
- Property/invariant tests cover allocation, destruction, reuse and capacity boundaries: covered by capacity loop for capacities 1 through 8 plus explicit registry/store/buffer capacity failures.
- Reviewer High fixed: `Int32ComponentStore.read(entity, registry)` and `has(entity, registry)` reject stale handles after direct `EntityRegistry.destroy` and slot reuse; covered by `rejects stale component reads after direct destroy and slot reuse`.
- Reviewer Medium fixed: `queueAttachInt32` and `queueSetInt32` validate signed 32-bit bounds before storing into the command buffer `Int32Array`; covered by `rejects queued Int32 command values before typed-array storage`.
- Reviewer Medium fixed: hot iteration callbacks receive primitive fields, and `StructuralCommandBuffer.commit` returns a reusable typed-buffer report inspected via `readStructuralCommandResult`; covered by `iterates active entities and attached components in ascending index order`, `exposes commit evidence through reusable typed result buffers`, and the `entity-store` benchmark.

## Checks run
| Command | Result | Artifact / notes |
|---|---|---|
| `pnpm install --frozen-lockfile` | PASS | Already up to date. |
| `pnpm format:check` | PASS | All matched files use Prettier code style. |
| `pnpm typecheck` | PASS | `tsc --noEmit --project tsconfig.typecheck.json --pretty false`. |
| `pnpm test --filter entity-store` | PASS | 1 file, 9 tests passed. |
| `pnpm bench --filter entity-store` | PASS | 8192 queued commands, 8192 reusable commit results, 8192 applied commands, 0 failed commands. |
| `pnpm lint` | PASS | `eslint . --max-warnings 0`. |
| `pnpm content:validate` | PASS | Content validation passed for 16 workspaces. |
| `pnpm boundaries:check` | PASS | Package boundary check passed for 16 workspaces. |
| `node tools/validate-handoff.mjs` | PASS | Handoff validation passed. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | PASS | Validation passed: 13 tasks, 10 roles. |
| `git diff --check` | PASS | No whitespace errors. |

## Determinism, save, performance and security impact
Determinism: entity iteration is ascending by index; structural commit order is explicit and deterministic. No ambient randomness, real time, DOM, React, Pixi, Electron, or filesystem access was added to `sim-core`.

Save: no save format or persistence changes. `EntityId` uses explicit `index` and `generation` fields suitable for future persisted references with generation validation.

Performance: registry, component store, and command buffer use bounded TypedArrays and explicit capacities. Entity and component stable iteration now pass primitive index/generation/value fields instead of allocating one `EntityId` object per visited entry. The command buffer uses an in-place insertion sort over bounded command slots during commit and records command outcomes into reusable typed result buffers instead of allocating one result object per command. Current changed sim-core TS line counts by `(Get-Content).Count`: `component-store.ts` 234, `entity-id.ts` 210, `entity-store.invariants.test.ts` 275, `index.ts` 67, `structural-command-types.ts` 187, and `structural-commands.ts` 398.

Security: no new runtime dependency, IPC, filesystem, Electron, or network surface.

## Decisions and deviations
- Stable structural command ordering is destroy, detach, attach, set, allocate; same priority uses entity index and then insertion sequence. This is intentionally exposed through per-command result sequences for testability.
- Destroy commit removes any component at the destroyed entity index only after generation validation succeeds, preventing stale destroy commands from clearing a new generation's component.
- Component store public read/has calls require an `EntityRegistry` so cross-tick references validate the authoritative live generation before exposing component data.
- Commit result evidence is exposed through `StructuralCommitReport` typed buffers plus caller-owned `StructuralCommandResultView` populated by `readStructuralCommandResult`; this keeps reviewer testability without per-command result object allocation.
- Spark was not used. Implementation was performed by the assigned `simulation-engineer`; no nested subagents were created.
- No `taskctl review`, `integrate`, or `done` command was run during this changes-requested repair. `taskctl complete` was rerun after project-director owner review to move WM-0007 back to `review_requested`.

## Remaining risks / follow-up tasks
- The initial component store is a foundational single-lane `Int32` SoA store. Future typed component schemas can build on the same active bitmap/generation pattern.
- Commit sorting is bounded by command buffer capacity and is suitable for this foundation; if future command buffers become large, add benchmark thresholds before changing the algorithm.
- No persistence round-trip coverage was added because save format is out of WM-0007 scope.

## Handoff to reviewer
Fastest reproduction path:

```bash
pnpm install --frozen-lockfile
pnpm format:check
pnpm typecheck
pnpm test --filter entity-store
pnpm bench --filter entity-store
pnpm lint
pnpm content:validate
pnpm boundaries:check
node tools/validate-handoff.mjs
node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate
git diff --check
```

Review focus: `packages/sim-core/src/component-store.ts`, `packages/sim-core/src/structural-commands.ts`, `packages/sim-core/src/structural-command-types.ts`, `packages/sim-core/src/entity-id.ts`, `packages/sim-core/src/entity-store.invariants.test.ts`, and `packages/benchmarks/src/index.ts`.
