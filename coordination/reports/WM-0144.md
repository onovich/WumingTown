# WM-0144 Work report

## Outcome

Repaired the desktop Electron E2E locale-title expectations and desktop reachability helpers that blocked the WM-0140 full `test:e2e` gate. The tests now assert the product title according to the active locale, keep mixed-language title regressions blocked, and sample reachable desktop UI controls inside the actually visible scroll clip instead of hidden overflow.

## Files and public contracts changed

- `apps/desktop-electron/src/desktop-shell.e2e.test.ts`: changed desktop title assertions to use active-locale expectations, made the tall/reachable hit tests clip-aware, treated the large-scale first-play guidance parent as a tall reachable surface while still requiring its inner cards to fit, and allowed a 2px Electron content-size rounding tolerance for desktop bottom bounds.
- `coordination/tasks/WM-0144.json`: task claim metadata from `taskctl claim`.

## Acceptance evidence

- Desktop start-surface and HUD title checks now respect active locale: English checks require `Wuming Town` and reject `无明镇`; zh-CN checks require `无明镇` and reject `Wuming Town`.
- Product localization behavior was not changed: no product code, locale fallback, settings persistence, or player-facing copy files were edited.
- Desktop E2E no longer fails on stale mixed-language title assumptions or hidden-clip hit testing: `apps/desktop-electron/src/desktop-shell.e2e.test.ts` passed 4/4.
- WM-0140 can resume the full visual evidence gate: full `corepack pnpm test:e2e` passed 3 files / 29 tests after this repair.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `corepack pnpm exec vitest run apps/desktop-electron/src/desktop-shell.e2e.test.ts` | Pass | 1 file, 4 tests passed. |
| `corepack pnpm test:e2e` | Pass | 3 files, 29 tests passed in 324.26s. |
| `node tools/validate-handoff.mjs` | Pass | 1386 files, 144 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | 144 tasks, 10 roles. |
| `git diff --check` | Pass | No whitespace errors. |
| `corepack pnpm quality` | Pass | typecheck, lint, format, localization, content, boundaries, M7 checklist, and 71 unit files / 394 tests passed. |

## Determinism, save, performance and security impact

Determinism: unchanged; no simulation, Worker, random stream, clock, or authoritative state touched.

Save: unchanged; no save schema, storage key, or persistence behavior touched.

Performance: unchanged; test-only assertions were edited.

Security: unchanged; no dependency, IPC, preload contract, filesystem bridge, telemetry, account, hosted service, or network behavior changed.

## Decisions and deviations

- Kept the repair in desktop E2E only after confirming the failures came from stale test assumptions and hit-test sampling, not from product localization code.
- The first-play guidance parent may be taller than a 390x720 large-scale desktop viewport; the test now treats that parent as tall reachable while still requiring `next goal`, `available actions`, `boundary`, language buttons, and settings controls to be individually viewport-bounded.
- Desktop normal and tall reachability helpers now follow the Web E2E pattern of sampling inside visible scroll clips.

## Remaining risks / follow-up tasks

- None blocking for WM-0144. WM-0140 must rerun its visual evidence branch after merging this fix.

## Handoff to reviewer

Inspect `apps/desktop-electron/src/desktop-shell.e2e.test.ts`, especially the active-locale title helper and clip-aware reachability helpers, then run `corepack pnpm exec vitest run apps/desktop-electron/src/desktop-shell.e2e.test.ts` or the full `corepack pnpm test:e2e`.
