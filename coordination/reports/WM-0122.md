# WM-0122 Work Report

## Outcome

Produced an independent M8 anomaly roster and content-expansion planning
artifact without touching the protected M5 baseline. The work remains
data/doc-only and keeps the M5 scenario, manifest and worker projection files
at HEAD/no-diff.

## Files changed

- `docs/04_content_balance/12_m8_anomaly_roster_spec.md`: new M8 roster/spec
  artifact with 12 accepted anomaly targets, 3 stretch targets, localization
  coverage expectations, scenario fixture expectations, data-mod policy
  alignment and WM-0123/WM-0124 handoff notes.
- `coordination/tasks/WM-0122.json`: task state metadata updated by
  workflow/taskctl during the claim/complete cycle.

## Acceptance evidence

- The protected M5 hashes remain unchanged and are explicitly recorded in the
  new spec as no-go baselines:
  `0xe55d3015`, `0x81d37435`, `0xfba70a5c`, `0x9ba83cb7`.
- The roster spec defines 12 accepted anomaly targets:
  Borrowed Shadow, Third Knock, Old Bridge Guest, Well Below Cry,
  Return-Lamp Keeper, Doorless Room, Name Counter, Dream-Borrowing Child,
  Ash Register, No-Guest Cart, Boundary Moth and Paper Rain.
- The spec also records 3 stretch targets:
  Market Scale, River Mirror Clerk and Last Watch Bell.
- Localization expectations are explicit for `en` and `zh-CN` keys, including
  label, description, review/counterevidence and scenario coverage.
- Scenario fixture expectations require prevention, misread, noncombat and
  review fixtures with stable anomaly identifiers.
- Data-mod policy alignment is documented as data-only, fail-closed and
  runtime-free for this task.
- Downstream handoff guidance is recorded for WM-0123 and WM-0124.

## Checks run

| Command | Result | Notes |
| --- | --- | --- |
| `pnpm test --filter m5-invariants` | Pass | Verified the protected M5 invariant/headless hash checks still pass. |
| `node tools/validate-handoff.mjs` | Pass | Handoff validation passed. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | Workflow validation passed: 127 tasks, 10 roles. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | Pass | Current task-state snapshot recorded before completion. |
| `git diff --check` | Pass | No whitespace errors. |
| `pnpm quality` | Pass | Typecheck, lint, format check, content validation, boundary check, M7 checklist and full unit test suite passed. |

## No-go confirmations

- No edits were left in the protected M5 files:
  `packages/sim-core/src/m5-alpha-content-scenario.test.ts`,
  `packages/sim-core/src/m5-invariants.test.ts`,
  `packages/sim-core/src/m5-save-replay.test.ts`,
  `packages/sim-core/src/m5-worker-projection.ts`,
  `packages/sim-worker/src/m5-worker-parity.test.ts`.
- No protected M5 fixture or hash was altered to satisfy WM-0122.
- No runtime, schema, Worker, save, UI, network, release, store, signing,
  telemetry, account or paid-service change was introduced.
- WM-0122 remains planning/content only until downstream tasks implement any
  actual roster data.

## Handoff

WM-0122 hands the roster spec to WM-0123 for faction/endgame arcs and to
WM-0124 for data-mod and validation workflow hardening.

## Reviewer note

Review the new roster spec and confirm the protected M5 baseline remains at
HEAD/no-diff. If a later implementation task is needed, it should consume this
spec rather than mutate the protected M5 content path.
