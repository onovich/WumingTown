# WM-0179 Work report

## Outcome

Implemented and validated the bounded structural-command ordering repair. The
product change replaces inversion-dependent insertion shifting with nine fixed
stable counting/radix passes over constructor-owned typed storage while
preserving the reviewed command-priority, signed Int32 entity-index and
insertion-sequence contract.

After WM-0180 was independently verified, integrated and closed, systems
architecture authorized one measured invocation of its reviewed Windows
PowerShell 5.1 quiet-host tool. That invocation passed at exact clean commit
`b762b70dca499c6e694c1fa71aed5d636b90aaa1`. It authorized benchmark attempt
one, which ran exactly once without pre-warm or retry and passed the unchanged
entity-store gate at a `1.5104000000000042 ms` median. WM-0179 is ready for
independent review.

## Files and public contracts changed

- `packages/sim-core/src/structural-command-order.ts`: adds an internal
  fixed-pass ordering owner with one ordered-slot array, one scratch-slot array
  and one 256-bucket array allocated in its constructor.
- `packages/sim-core/src/structural-commands.ts`: delegates queued-slot ordering
  to the constructor-owned internal helper; public queue, commit and result APIs
  are unchanged.
- `packages/sim-core/src/entity-store.invariants.test.ts`: adds exact mixed
  priority, signed invalid-index, reverse-index, same-index stability, capacity
  and consecutive-reuse evidence.
- `coordination/artifacts/WM-0179/admission/**`: preserves the one measured
  admission JSON and its SHA-256 sidecar.
- `coordination/artifacts/WM-0179/benchmarks/**`: preserves the one canonical
  entity-store benchmark JSON and its SHA-256 sidecar.
- `coordination/tasks/WM-0179.json` and `coordination/reports/WM-0179.md`:
  durable workflow state and final evidence.

Relative to `main`, the package diff is exactly the three authorized files
above. `packages/sim-core/src/entity-id.ts`,
`packages/sim-core/src/component-store.ts`, `packages/benchmarks/**`, public
exports, protocols, save/snapshot formats, dependencies and downstream task
surfaces remain unchanged.

## Acceptance evidence

- Ordering is fixed at nine stable passes: four Uint32 insertion-sequence byte
  passes, four signed Int32 index byte passes using `index ^ 0x80000000`, then
  one canonical command-priority pass.
- `StructuralCommandOrder` owns all two-slot-array and bucket storage from
  construction. The ordering path clears, counts, prefix-sums, distributes and
  swaps typed-array references; it contains no `Array.sort`, comparison sort,
  insertion shifting, closure or per-commit object/array/typed-array/string
  construction.
- Focused coverage is 11/11 passing. The exact historical mixed-command result
  sequence remains `[6, 2, 3, 7, 8, 4, 5, 9, 0, 1]`; consecutive reverse/reuse
  commits retain `[3, 2, 1, 0]` and `[6, 4, 5, 7]`.
- M1, M2 and M3 100000-tick runs retain world hashes `0xf7815189`,
  `0x6943dad4` and `0x7eb81a69`, with printed terminal invariants clean and the
  M3 replay hash matching.

### Quiet-host admission

The reviewed command was invoked exactly once with repository root
`D:\WebProjects\WumingTown`, exact expected commit
`b762b70dca499c6e694c1fa71aed5d636b90aaa1` and the explicit
no-concurrent-benchmark confirmation. The child process used distinct unique
TEMP stdout/stderr files and exited `0`; stdout contained exactly one compact
measured JSON line and stderr was empty.

The fail-closed contract validated `schemaVersion=1`,
`evidenceKind=measured`, `status=pass`,
`reasonCode=admission_passed`, JSON/process exit `0`, unchanged exact commit,
clean tree before and after, constants `2` windows / `6` seconds / `40%` host /
`8%` maximum process, two finite passing windows, all ACShadows absence facts
and `noConcurrentBenchmarkConfirmed=true`.

| Window | Host busy | Maximum process share | Maximum process |
| ---: | ---: | ---: | --- |
| 1 | `19.190668%` | `6.256417%` | `Code` PID `40096` |
| 2 | `20.434959%` | `6.215091%` | `Code` PID `40096` |

Preserved admission artifact:

- `coordination/artifacts/WM-0179/admission/quiet-host-admission.json`
- SHA-256
  `2EBDFBC434D8433D61FF50B3B9921D0FE37D0A13E70299F9A350DEB0F5A35F29`

### Canonical entity-store benchmark

`corepack pnpm bench --filter entity-store` was invoked exactly once after the
admission PASS, with `WM_ARTIFACT_DIR=coordination/artifacts/WM-0179`. It exited
`0` and reported `OK`:

| Fact | Result |
| --- | ---: |
| samples / warmups | `9 / 2` |
| median | `1.5104000000000042 ms` |
| maximum permitted median | `293.4084 ms` |
| unchanged baseline | `244.507 ms` |
| warning / failure thresholds | `10% / 20%` |
| capacity | `8192` |
| queued commands | `8192` |
| commit results | `8192` |
| applied / failed commands | `8192 / 0` |
| attached components | `8192` |
| iteration checksum | `134209536` |
| invariant mismatches | `0` |

The JSON contains exactly one `entity-store` result, the exact git commit and
the locked workload facts. Its conventional SHA-256 sidecar names
`benchmark-results.json`; the recorded and actual hashes both equal
`41E92F8FD16A05DEC269CC8C47630844DCA78C371511BB1AB210629354698F36`.

## Checks run

| Command | Result | Artifact / notes |
| --- | --- | --- |
| reviewed `quiet-host-admission.ps1` production invocation | PASS | Exactly once; exit `0`, one measured JSON stdout line, empty stderr, two finite passing windows |
| `corepack pnpm bench --filter entity-store` | PASS | Exactly once; median `1.5104000000000042 ms`, 9/2, all invariants match |
| benchmark JSON plus SHA-256 sidecar validation | PASS | Actual and recorded hash `41E92F...98F36` |
| `corepack pnpm typecheck` | PASS | Final product contents |
| `corepack pnpm exec vitest run packages/sim-core/src/entity-store.invariants.test.ts` | PASS | 1 file, 11 tests |
| `corepack pnpm sim:run -- --seed 1 --scenario hauling-building --ticks 100000` | PASS | `0xf7815189`; invariants clean |
| `corepack pnpm sim:run -- --seed 5 --scenario m2-work-logistics --ticks 100000` | PASS | `0x6943dad4`; invariants clean |
| `corepack pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 100000` | PASS | `0x7eb81a69`; replay match and terminal counters clean |
| `corepack pnpm boundaries:check` | PASS | 16 workspaces |
| `node tools/validate-handoff.mjs` | PASS | 1672 files, 180 tasks, 10 roles; digest `b9862c05849086178b74060ba7759b08b2081e1692c33da97e2a91e06946d6d5` |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | PASS | 180 tasks, 10 roles |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | PASS | WM-0179 is the sole in-progress task; downstream blockers unchanged; unread inbox `0` |
| `git diff --check` | PASS | No whitespace errors before final evidence commit |
| `corepack pnpm quality` | PASS | Typecheck, lint, format, localization, content, boundaries, M7 checklist and unit tests passed |

## Determinism, save, performance and security impact

- Determinism: priority/index/sequence ordering remains exact; focused
  mixed/reuse cases and all three 100000-tick hashes pass.
- Save: no save format, schema, migration or snapshot surface changed.
- Performance: inversion-dependent quadratic shifting is replaced by nine
  fixed stable passes over reusable storage. The unchanged canonical filter
  passes by a wide margin without baseline, threshold, workload or retry-policy
  changes.
- Security: no dependency, filesystem, network, IPC, Electron or privilege
  surface changed. Admission evidence records only reviewed repository/runtime
  and process-load facts.

## Decisions and deviations

The final run follows the systems-architect authorization at control commit
`b762b70`: one reviewed-script admission and, only after its complete measured
PASS, one entity-store benchmark attempt. No inline admission, synthetic probe,
pre-warm, retry, waiver, threshold/baseline change or scope expansion occurred.
The earlier failed inline attempts remain historical evidence; neither had
invoked a benchmark, so this was benchmark attempt one.

## Remaining risks / follow-up tasks

- Independent review must verify algorithmic boundedness, constructor-owned
  scratch, exact ordering parity, allowed paths, the one-run policy and all
  recorded artifacts/checks.
- Project-director integration and `done` are still required. Only then may
  WM-0178 resume its separately owned six-filter and full canonical gate.
  WM-0177 and WM-0170 remain blocked behind their existing DAG.

## Handoff to reviewer

Inspect the full branch diff against `main`, especially the three authorized
package files and the two admission/benchmark artifact pairs. Recompute both
SHA-256 sidecars, verify the one-line admission JSON and benchmark invariants,
then independently run the non-performance gates needed for review. Do not
rerun the admission or benchmark; their one-attempt evidence is immutable.
