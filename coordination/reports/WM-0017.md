# WM-0017 Work report

## Outcome

Proposed `ADR-0004: M1 Worker snapshot and read-model encoding` for review. The
ADR defines versioned read-only render snapshots, UI detail/read-model behavior,
sequence and stale result handling, backpressure, metrics fields, and future
Node/Worker parity verification. No protocol/runtime code, runtime dependency,
UI mutation authority, or SharedArrayBuffer/deployment requirement was added.

## Files and public contracts changed

- `coordination/decisions/ADR-0004.md`: new Proposed ADR for M1 Worker snapshot
  and read-model encoding.
- `docs/05_tech/07_architecture_decision_records.md`: linked ADR-0004 as
  Proposed while preserving existing ADR-0003 and ADR-0005 Proposed entries.
- `coordination/reports/WM-0017.md`: task evidence and reviewer handoff.

## Acceptance evidence

- ADR created with `Status: Proposed`: `coordination/decisions/ADR-0004.md`.
- Alternatives, migration, rollback, test, and performance implications are
  covered in `Considered options`, `Verification`, `Performance implications`,
  and `Rollback / migration`.
- Snapshot/read-model ownership keeps Simulation Worker and Node headless
  authoritative while UI, Pixi, React, and Electron remain read-only consumers:
  ADR sections `Decision`, `UI summaries and detail behavior`, and `Contracts
introduced`.
- Versioning, sequence handling, backpressure, metrics fields, stale result
  rejection, and read-only UI detail behavior are specified in dedicated ADR
  subsections.
- Worker and Node parity tests required by later tasks are specified in
  `Verification` and `Downstream implementation dependencies`.

## Checks run

| Command                                                                                                                                                                                                                                                                 | Result                                                  | Artifact / notes                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------- |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate`                                                                                                                                                                                           | PASS                                                    | `Validation passed: 30 task(s), 10 role(s).`                |
| `node tools/validate-handoff.mjs`                                                                                                                                                                                                                                       | PASS                                                    | `Handoff validation passed: 413 files, 30 tasks, 10 roles.` |
| `git diff --check`                                                                                                                                                                                                                                                      | PASS                                                    | No whitespace errors.                                       |
| `pnpm exec prettier --check coordination/decisions/ADR-0004.md coordination/reports/WM-0017.md docs/05_tech/07_architecture_decision_records.md coordination/tasks/WM-0017.json coordination/thread-registry.json coordination/inbox/reviewer/MSG-MQRMIHF6-8A0054.json` | PASS after formatting `coordination/tasks/WM-0017.json` | Targeted Markdown/JSON formatting check.                    |

## Determinism, save, performance and security impact

Determinism: ADR-only. The proposal requires canonical world/read-model hashes,
stable render lane ordering, versioned sequence handling, and Node/Worker parity
tests for later WM-0028 work.

Save: no save format or persistence code changed. ADR-0004 explicitly keeps
render snapshots and UI details non-authoritative and defers save container
semantics to ADR-0005.

Performance: no benchmark baseline changed. The ADR requires later metrics and
benchmarks for snapshot size, encode latency, dropped snapshots, queue depth,
backpressure, and stale rejection counters.

Security: no dependency, IPC, filesystem, Electron, network, or deployment
surface changed. The ADR does not require SharedArrayBuffer or cross-origin
isolation changes.

## Decisions and deviations

ADR-0004 remains Proposed and awaits independent review. Spark was not used:
WM-0017 is an architecture/Worker protocol decision task and is explicitly
outside the rapid implementation lane.

The task was already claimed by `systems-architect` in this worktree when work
started. Re-running `taskctl claim` returned `Cannot claim WM-0017 from state
in_progress`; the existing claim/thread metadata was preserved.

## Remaining risks / follow-up tasks

- Reviewer must decide whether ADR-0004 is acceptable before WM-0028
  save/replay and Worker/headless parity implementation proceeds.
- Downstream protocol tasks still need exact TypeScript types, validators,
  canonical hash helpers, and Worker/Node test fixtures.
- Snapshot cadence and byte budgets remain implementation-time measurements;
  this ADR defines required metrics rather than fixed thresholds.

## Handoff to reviewer

Review `coordination/decisions/ADR-0004.md` first, then confirm the ADR index
entry in `docs/05_tech/07_architecture_decision_records.md`. Re-run the checks
listed above from the repository root.
