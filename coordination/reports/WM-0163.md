# WM-0163 Work report

## Outcome

`@wuming-town/sim-core` now exports a protocol-neutral authoritative
`GameSessionRuntime`, PR-1 `ScenarioDefinition` initializer and headless scenario
entry. The runtime owns fixed 30 TPS integer advancement, seed/named random
streams, stable command ordering, the minimum PR-1 owner graph, bounded indexed
work/path probes, canonical world/read-model hashes, read-only projection basis
builders, metrics and structured conservation diagnostics. The exact
`pr1-integrated-gamesession` headless alias delegates to that package-root entry.

## Files and public contracts changed

- `packages/sim-core/src/game-session-types.ts`: public constants and runtime,
  command, basis, projection, metrics, conservation and scenario types.
- `packages/sim-core/src/game-session-definition.ts`: focused immutable 64x64
  PR-1 `GameSessionScenarioDefinition` factory.
- `packages/sim-core/src/game-session-initializer.ts`: owner graph construction,
  definition validation and structured initialization result.
- `packages/sim-core/src/game-session-owner-setup.ts`: stable entity/resource/
  bed/lamp/build setup and initial derived-index rebuild.
- `packages/sim-core/src/game-session-residents.ts`: the missing resident identity,
  activity, current-job and structured-reason typed owner lanes.
- `packages/sim-core/src/game-session-command-queue.ts`: fixed-capacity stable
  `(tick, sequence)` queue and canonical command hash entries.
- `packages/sim-core/src/game-session-tick.ts`: stable eight-phase tick coordinator
  with bounded dirty/index/work/path processing.
- `packages/sim-core/src/game-session-hash.ts`: canonical authoritative and
  read-model hash inputs, excluding presentation state.
- `packages/sim-core/src/game-session-projection.ts`: protocol-neutral read-only
  render/UI basis builders.
- `packages/sim-core/src/game-session-diagnostics.ts`: jobs/reservations/queues/
  resources conservation report and first-divergence diagnostics.
- `packages/sim-core/src/game-session-runtime.ts`: public runtime lifecycle and
  composition surface.
- `packages/sim-core/src/game-session-scenario.ts`: package-root headless scenario
  entry.
- `packages/sim-core/src/game-session.test.ts`: initializer, determinism, phase,
  projection, 100000-tick conservation and headless parity coverage.
- `packages/sim-core/src/index.ts`: package-root value/type exports.
- `tools/headless-runner/src/index.ts`: exact PR-1 alias registration and package-
  root delegation only.
- `tools/headless-runner/src/index.test.ts`: accepted dispatch and unknown-alias
  rejection coverage.
- `coordination/tasks/WM-0163.json`: taskctl unblock/completion control state.

Exact package-root entry points:

```ts
import {
  GameSessionRuntime,
  createPr1IntegratedGameSessionDefinition,
  initializeGameSessionRuntime,
  runPr1IntegratedGameSessionScenario,
  type GameSessionRenderProjection,
  type GameSessionUiProjection,
} from "@wuming-town/sim-core";
```

Primary runtime methods are `queueCommand`, `advanceTicks`, `setPaused`,
`setRequestedSpeed`, `rebuildDerivedIndexes`, `createCanonicalWorldHashInput`,
`createWorldHash`, `createReadModelHash`, `createRenderProjection`,
`createUiProjection`, `createMetrics`, `createConservationReport` and
`readLastPhaseOrder`. `runtime.owners` exposes the composed sim-core owner graph;
`runtime.indexBasis` names the current owner/index/path version basis.

## Reused stores and adapted scenario facts

| Existing sim-core surface | PR-1 use |
|---|---|
| `EntityRegistry` | Stable `EntityId(index,generation)` allocation for all 26 initial entities. |
| `MapGrid` | 64x64, chunk size 8, all 4096 cells navigable; integer map/version basis. |
| `LocationStore` / `SpatialIndex` | 21 map members, four contained item stacks and stable direct position queries. |
| `ReservationLedger` | Sole active-claim authority and leak/conflict metrics; starts/drains at zero. |
| `JobCoreStore` | Explicit serializable job owner surface and terminal metrics; no implicit job position. |
| `WorkOfferIndex` / `ReasonTraceStore` | Four storage supply plus two build material offers; one resident/300 ticks bounded exact-bucket probe. |
| `GridPathfinder` / `PathRequestBatcher` | Versioned one-at-a-time exact path request from selected indexed work; stale commit rejection remains active. |
| `ItemStackStore` / `StorageLogisticsIndex` | Food 80, wood 60, stone 50, lamp oil 40; separate storage and contained stack entities. |
| `BuildSiteStore` | One simple shelter site requiring 20 wood, 15 stone and 300 work ticks. |
| `NeedStore` / `NeedUrgencyIndex` | Eight resident need rows, stable phased decay and bounded dirty refresh. |
| `RestSleepStore` / `RestCandidateIndex` | Eight indoor night bedroll fixtures and a versioned rebuild path. |
| `M3EnvironmentStore` | Integer day/night/weather owner progression at 1500-tick boundaries; dirty keys drain bounded. |
| `M4LampNetworkStore` | One fueled road/home lamp with owner version and bounded dirty projection key. |
| `NamedRandomStreams` / canonical world hash | Named deterministic stream state plus stable command and owner fields. |

Adapted facts come from M1/M2 storage/build concepts, M3 resident needs/rest/day-
night concepts and M4 lamp ownership. No historical M1-M8 scenario runner is
called by the runtime or initializer. The WM-0150 command slice is not reused as
authority.

## Acceptance evidence

- Package-root runtime/initializer: `game-session.test.ts` creates the runtime
  exclusively through `initializeGameSessionRuntime` imported from `./index`.
- Initial world: tests verify 64x64, every cell passable, eight residents/eight
  need rows/eight beds, four item/storage lanes, one lamp, one build site, 26
  live entities, 21 map memberships and four container memberships.
- Fixed phases: every tick records exactly `GAME_SESSION_PHASE_ORDER`; 100000
  ticks report zero phase violations and zero full-world Pawn scans.
- Determinism: equal seed/command streams produce equal world/read-model hashes
  and metrics; same-tick command reordering produces a different world hash.
- Projection: render and UI builders are read-only, share tick/snapshot/world/
  read-model/owner-version basis and expose 22 stable render rows/eight resident
  UI rows/four resource rows without protocol imports.
- Conservation: the 100000-tick test queues commands at ticks 0 and 99999 and
  ends with zero jobs, reservations, pending commands/paths and every derived
  backlog; all four resources have delta zero. A forced food mutation reports
  `game_session.resource_conservation_failed` with tick, def id, expected and
  actual quantities.
- CLI boundary: focused tests accept only `pr1-integrated-gamesession`, reject an
  extended unknown alias and call `runPr1IntegratedGameSessionScenario` from the
  sim-core package root. `cli.ts` remains unchanged.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `corepack pnpm typecheck` | PASS | Strict workspace typecheck. |
| Required focused Vitest command | PASS | 8 files, 62 tests. |
| `corepack pnpm sim:run -- --seed 5 --scenario pr1-integrated-gamesession --ticks 18000` | PASS | Tick 18000; world `0x1dd38089`; read model `0xd76e7a49`; 60 accepted paths; zero leaks/backlogs. |
| Extra 100000-tick CLI evidence | PASS | Tick 100000; world `0xfcab9681`; read model `0x8c739c09`; 334 accepted paths, 6853 node expansions, zero stale/leaks/backlogs/resource delta; command wall run completed in about 0.63 s including Node startup. |
| `corepack pnpm boundaries:check` | PASS | 16 workspaces. |
| `node tools/validate-handoff.mjs` | PASS | 1568 files, 166 tasks, 10 roles; digest `4a35b635...2583ccd`. |
| `taskctl validate` | PASS | 166 tasks, 10 roles. |
| `taskctl status` | PASS | WM-0163 is the sole in-progress task before completion. |
| `git diff --check` | PASS | No whitespace errors. |
| `corepack pnpm quality` | PASS | 75 test files, 421 tests; typecheck/lint/format/localization/content/boundaries/checklist all pass. |

## Determinism, save, performance and security impact

- Determinism: fixed 30 TPS integer ticks, stable numeric initialization,
  non-decreasing command ticks with insertion sequence tie-break, named seeded
  streams, versioned path basis and canonical hashes. No real clock,
  `Math.random`, Node global, Promise/coroutine job state or hidden singleton.
- Performance: normal work selection touches one resident and one exact indexed
  bucket every 300 ticks, with one queued exact path and a 256-node cap. The
  100000-tick run reports 334 visits/paths, 6853 node expansions and zero global
  Pawn scans. Publication-time projection/hash allocation is outside hot tick
  loops.
- Save/load: no snapshot API, public save container, schema, migration or
  compatibility promise is added. Future save authority remains owner stores,
  tick, random streams and command tail; derived indexes/projections remain
  rebuild-only/read-only.
- Security/boundaries: sim-core imports no DOM, React, Pixi, Electron, Node file
  system, Worker protocol or Web code. The tool adapter imports only the sim-core
  package root.

## Decisions and deviations

- Follows `ADR-0017`; no protocol/schema/message/save change was required.
- Keystone's blocker fix `231956f` added only the exact headless registry/test
  paths. Implementation did not modify `cli.ts`, package metadata or other tools.
- The default build anchor is `(24,0)` so the existing focused `BuildSiteStore`
  anchor-cell convention remains coherent without modifying that protected
  historical store. General arbitrary-Y build placement is intentionally not
  claimed by this scaffold.
- An intermediate CLI run exposed Node strip-only rejection of TypeScript
  parameter properties. The two internal constructors were rewritten with
  explicit fields; final CLI and all gates pass.
- `packages/sim-core/src/runner.ts` and historical runner fixtures were left
  unchanged because the new package-root scenario entry advances the exact same
  `GameSessionRuntime` surface directly.

## Remaining risks / follow-up tasks

- Gameplay command semantics beyond deterministic `noop` ordering are not
  connected; the queue is a fixed 128-entry PR-1 scaffold, not a save contract.
- Hauling, build delivery/construction, lamp refuel, rest/eating/medical drivers
  and resource production/consumption are present as historical stores but are
  not scheduled into this minimum runtime. Residents therefore expose the
  structured `game_session.indexed_work_available` / awaiting-driver state.
- Health, ability, mood/thought, relationships, schedules beyond the minimum
  environment owner, Chronicle/evidence/obligation/town rules/crises/director,
  M5 content and M8 faction/endgame remain regression-only/unconnected.
- Normal wall-time scheduling, schema-v3 protocol mapping, reliable Worker
  delivery/backpressure and Web rendering are WM-0164/WM-0165 work. Sim-core
  intentionally owns no timer.
- No GameSession load/save implementation exists. Public compatibility remains
  owner-gated.

## Handoff to reviewer

Fastest inspection path:

1. Read `packages/sim-core/src/game-session-runtime.ts`,
   `game-session-initializer.ts`, `game-session-tick.ts` and
   `game-session.test.ts`.
2. Run the required focused Vitest command and the 18000-tick CLI command from
   `coordination/tasks/WM-0163.json`.
3. Inspect `tools/headless-runner/src/index.ts` to confirm it contains only the
   exact alias and package-root dispatch.

WM-0164 should import `initializeGameSessionRuntime`, `GameSessionRuntime`,
`GameSessionRenderProjection` and `GameSessionUiProjection` from
`@wuming-town/sim-core`. The Worker host owns one runtime instance, drives
`advanceTicks(integerTicks)` from its external scheduler, maps
`createRenderProjection()` / `createUiProjection()` into the separately reviewed
protocol contract, and keeps reliable transport concerns outside sim-core. It
must not call historical scenario runners, copy owner state or treat core
projections as save authority.
