# WM-0060 Work report

## Outcome

M3 ordinary-life simulation is closed as a reviewed simulation capability
milestone. WM-0046 through WM-0059 are done, independently verified, integrated
and pushed; project state and roadmap documents now reflect M3 closeout; and
`coordination/reports/WM-0060-future-m4-entry-prompt.md` records a
non-executable future M4 planning handoff. No M4 task was created, promoted,
claimed or implemented.

## Files and public contracts changed

- `docs/07_roadmap/00_roadmap.md`: adds the M3 closeout addendum and explicit
  M4-unstarted boundary.
- `docs/07_roadmap/01_milestones_and_quality_gates.md`: adds M3 closeout gate
  evidence, known warning status and residual non-scope.
- `docs/07_roadmap/08_m3_ordinary_life_simulation_plan.md`: changes status to
  closed by WM-0060 and records final evidence.
- `coordination/project-state.json`: marks project phase as `m3-closed` and
  active milestone as M3 closed / M4 planning not started.
- `coordination/reports/WM-0060.md`: records closeout evidence and checks.
- `coordination/reports/WM-0060-future-m4-entry-prompt.md`: stores a future,
  non-executable M4 planning prompt only.
- `coordination/tasks/WM-0060.json`: task state updated by taskctl claim and
  completion workflow.

## Acceptance evidence

- Upstream M3 tasks are closed: WM-0046 through WM-0059 are all `done`, each
  has `review.verdict = verified`, and each has project-director integration
  metadata.
- Final M3 scenario evidence: `pnpm sim:run -- --seed 3 --scenario
m3-ordinary-life --ticks 100000` passes with scenario id
  `m3.ordinary_life.injured_caregiver.v1`, requested seed `3`, authoritative
  scenario seed `46`, command hash `0x226832d2`, content hash `0xdfe7107e`,
  final world hash `0x7eb81a69`, replay hash match `true`, zero terminal
  reservation/job leaks, zero stale medical offer rejects, zero stale ability
  cache rejects, zero active M4 facts and zero conservation drift.
- Save/replay evidence: WM-0057 verifies save tick `12000`, load tick `12001`,
  resumed final world hash `0x9b04b712`, final read-model hash `0x0f12213c`,
  loaded state hash `0x0dbf661c`, save bytes `4941`, rebuilt derived surfaces,
  `firstDivergentTick: null`, and artifact path
  `coordination/artifacts/WM-0057/m3-save-replay/summary.json`.
- Worker parity evidence: WM-0058 verifies Node Worker and real browser module
  Worker M3 parity for checkpoints `0`, `3600`, `7200`, `12000`, `18000` and
  `36000`; render snapshots, UI deltas, metrics and save metadata are read-only
  projections through the existing Worker protocol.
- Benchmark and invariant evidence: WM-0059 verifies
  `m3-ordinary-life-long-run` through `100000` ticks with final world hash
  `0x7eb81a69`, final read-model hash `0x82bf87d6`, Worker projection bytes
  `1747`, exact stable condition/mood/relationship invariants, zero stale
  offers, zero queue growth and zero hash divergence. The reviewed benchmark
  artifact is
  `coordination/artifacts/WM-0059/benchmarks/benchmark-results.json` with
  SHA-256 `63FAEA795D04934838C306DC99D9AA1152B56D52C38D123C3442DB496F82CEC3`.
- Final benchmark policy: thresholds remain 10 percent warning and 20 percent
  blocking. The final integration benchmark comparison had M3 status `OK`
  with median `25.073ms`, baseline `24.456ms`, regression `2.52%`, and no
  invariant mismatch. A `map-dirty` warning at `16.87%` was non-blocking.
- Future M4 prompt: `coordination/reports/WM-0060-future-m4-entry-prompt.md`
  is explicitly non-executable and says it must not be used to start M4 during
  WM-0060.
- M4 remains unstarted: no `coordination/tasks/WM-0061*.json` file exists, no
  M4 task is promoted/claimed, and this closeout changes no packages, apps,
  tools, schema, protocol, dependency, save format or runtime behavior.

## Checks run

| Command                                                                       | Result | Artifact / notes                                                                                                                                                                         |
| ----------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm ci:local`                                                               | Passed | 37 unit files / 205 tests and 3 e2e files / 13 tests passed; handoff and coordination validation passed                                                                                  |
| `pnpm bench`                                                                  | Passed | all benchmarks OK; M3 local median `22.443ms` vs baseline `24.456ms`; generated artifact restored to reviewed SHA-256 `63FAEA795D04934838C306DC99D9AA1152B56D52C38D123C3442DB496F82CEC3` |
| `pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 100000`         | Passed | final world hash `0x7eb81a69`; replay hash match `true`; zero active M4 facts and zero terminal leaks                                                                                    |
| `node tools/validate-handoff.mjs`                                             | Passed | 750 files, 60 tasks and 10 roles validated                                                                                                                                               |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Passed | 60 tasks and 10 roles validated                                                                                                                                                          |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status`   | Passed | 1 task in progress, 59 done, 0 unread inbox messages before WM-0060 completion                                                                                                           |
| `git diff --check`                                                            | Passed | no whitespace errors                                                                                                                                                                     |
| `pnpm quality`                                                                | Passed | typecheck, lint, format check, content validation, boundary check and 37 unit files / 205 tests passed                                                                                   |

## Determinism, save, performance and security impact

Determinism: WM-0060 changes documentation and coordination state only. It
records deterministic M3 hashes and does not alter simulation code.

Save: no save format, migration, compatibility promise or platform save UI is
changed. The future M4 prompt explicitly blocks future save/public protocol
changes unless separately reviewed.

Performance: no threshold is changed. WM-0060 records the reviewed M3 benchmark
artifact and known non-blocking benchmark warning status.

Security/authority: UI, Pixi, React, Electron and Worker projection consumers
remain read-only. No app, preload, platform or protocol surface is edited.

## Decisions and deviations

- WM-0060 deliberately does not create the next M4 task DAG. The future prompt
  is a planning handoff artifact only.
- `pnpm bench` is a required closeout command but writes the tracked WM-0059
  benchmark artifact. WM-0060 records the command result and restores the
  reviewed WM-0059 artifact before committing, because WM-0060 allowed paths do
  not include benchmark artifacts.

## Remaining risks / follow-up tasks

- Benchmark timings remain machine-sensitive. The current policy treats 10
  percent as warning and 20 percent as blocking; M3 was OK in the reviewed
  artifact and final integration run.
- M4 needs a fresh reviewed planning task before any implementation. The future
  prompt is intentionally insufficient to authorize work by itself.

## Handoff to reviewer

Review the closeout deltas only: roadmap/project-state/report files and the
future M4 prompt. Confirm WM-0046 through WM-0059 are done and verified, run
the required checks, and verify no M4 task was created or implemented.
