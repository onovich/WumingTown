# WM-0168 Work report

## Outcome

PASS. A real Chromium module Worker remained passive after one schema-v3 `InitSession` for 600092.8 browser-monotonic milliseconds and advanced from tick 0 to tick 18000. The exact full Worker smoke passed 19/19, the default Web shell passed 13/13 at 1424x861, and no production, protocol, schema, projection or save file changed.

## Real Worker artifact

Artifact: `coordination/artifacts/WM-0168/real-worker-600s.json`.

- Input: one `InitSession`; no subsequent command, UI advance, wait, drain, command-drain or direct `runScheduledQuantum`.
- Elapsed: 600092.8 ms; tick 0 -> 18000; 6001 coherent UI publications.
- Minute ticks: 1803, 3603, 5403, 7200, 9000, 10803, 12600, 14400, 16200, 18000.
- Maximum publication gap: 128.3 ms, below the 5000 ms hard gate.
- Lifecycle: idle@0, moving@3, moving@6, working@9, working@12, idle@15. Every row has strictly increasing snapshot sequence/tick and its exact world/read-model hashes in the artifact.
- Backpressure/health: dropped snapshots 0, maximum reliable queue depth 2, fatal messages 0, observed pre-cleanup closed lifecycle states 0, coherence failures 0. Lifecycle subscription recorded `created -> initializing -> active`; the explicit test cleanup separately recorded `destroyed`.
- Existing focused forced-backpressure test also confirms latest-wins snapshots and ordered reliable delivery.

## Default Web evidence

The 1424x861 default-route E2E uses a Vite test-only transform to record the exact `nextReadModel` immediately after production calls `activeRenderer.setReadModel(nextReadModel)`, paired with the accepted `gameSessionFrame.basis`. It therefore observes the product assembler -> `WorldReadModel` -> Pixi consumption path, not raw Worker messages. It proves separate consumed moving ticks 3/6 and later working ticks 9/12, then continues through the normal active shell. Existing assertions retain `projectionSource=game-session-worker`, 22 render rows, eight residents, four resources, selection details, same-session basis, schema-v3 request, no command-loop clock and mechanical exclusion of fixture/advance/wait/drain authority. Full Web shell result: 13/13 PASS.

## Required checks

| Check | Result |
|---|---|
| `corepack pnpm typecheck` | PASS |
| focused protocol/core/Worker/Web Vitest | PASS, 4 files / 44 tests |
| `node tools/test-runner.mjs e2e --filter worker-smoke` | PASS, 19/19 including exact 600-second gate |
| `node tools/test-runner.mjs e2e --filter web-shell` | PASS, 13/13 |
| 18000-tick PR-1 run | PASS; world `0x695bd812`, read `0x84ecbaa1`, zero leaks/backlogs/delta |
| 100000-tick PR-1 run | PASS; world `0xa268d08d`, read `0xaaa21e3e`, zero leaks/backlogs/delta |
| boundaries / handoff / taskctl validate/status / diff-check | PASS |
| `corepack pnpm quality` | PASS; 78 files / 462 tests plus lint, format, localization, content, boundaries and checklist |

Web E2E regenerated WM-0135 responsive evidence; the out-of-scope drift was inspected and restored.

## Evidence-run notes

The first 600-second run passed elapsed and tick gates but exposed a QA collector boundary race that recorded only nine minute rows. The collector was fixed to use the terminal publication for the tenth boundary. A later full run reached elapsed 600000 ms at tick 17997; the runner had ended on wall time alone. Its completion condition was corrected to require both unchanged hard gates (`elapsed >= 600000` and `tick delta >= 18000`). The final exact run passed. No product behavior or threshold was changed.

The rejected Web observer wrapped `Worker.prototype.addEventListener` and was order-sensitive across the full suite. It has been removed entirely. The final evidence instruments only the test-served `activeRenderer.setReadModel` call, never patches Worker construction/prototypes, sends messages or changes authority. Targeted and full Web runs pass.

The real Worker collector now subscribes to the public browser-session lifecycle. `closedCount` is computed from actually observed `fatal`/`shutdown`/`destroyed` states before cleanup rather than a constant. The artifact records zero pre-cleanup closures, the exact active lifecycle sequence, and the separately observed cleanup state `destroyed`; fatal message count remains independent.

## Scope and residual risk

- Evidence is local Chromium on this Windows host; OS suspension/background throttling on other hosts remains platform variability, while the measured run stayed well within the cadence gate.
- No GameSession save/load compatibility is introduced.
- No release, telemetry, account, dependency or PR-2+ work was performed.
- WM-0166 remains blocked until WM-0168 is independently verified, integrated and done, after which its exact resume procedure must rerun WM-0166 rather than reuse a stale pass conclusion.
