# WM-0090 Work report

## Outcome
WM-0090 produces a reproducible Windows Electron unsigned unpacked external-test directory with machine-readable package evidence. The build opens the current product shell and M5 product-gate fixture, records artifact metadata, and keeps Electron as a sandboxed platform shell rather than simulation authority.

## Files and public contracts changed
- `apps/desktop-electron/package-dir.ts`: writes `dist/desktop/wm-desktop-package-report.json` after packaging with artifact paths, package kind, Electron version, file count, total bytes, content digest, package metadata, security-boundary facts and known warnings.
- `apps/desktop-electron/src/desktop-shell.e2e.test.ts`: verifies the package report, packaged `file:///` launch path, renderer sandbox, placeholder platform ports and M5 product-gate fixture surface.
- `docs/05_tech/06_platform_matrix.md`: records WM-0090 Windows package status and scope boundaries.
- `coordination/reports/WM-0090-plan.md`: execution plan for the task.
- `coordination/tasks/WM-0090.json`: claim state and task workflow history.

No dependency, lockfile, installer, signing, updater, store, public upload, save schema, public protocol or simulation-authority contract changed.

## Acceptance evidence
- Windows package or unpacked external-test build is reproducible: two consecutive `pnpm build:desktop` runs produced `dist/desktop/win-unpacked/WumingTown.exe` and the same package digest `a88b28f6230ae2494fccccb9db44cebecc203bf1e7aae62011467bbde59b2f15`.
- Build opens the current product shell and M5 scenario surfaces: `pnpm test:e2e --filter desktop-shell` launches the packaged Windows directory, confirms the renderer loads from `file:///`, and asserts `wm-0086-web-product-gate` plus `m5.alpha_content_framework.first_season.v1` are visible in the shell.
- Package metadata, artifact path, size and known warnings are recorded: `dist/desktop/wm-desktop-package-report.json` records `packageKind: "windows-unpacked-directory"`, Electron `42.4.1`, `fileCount: 91`, `totalBytes: 375111853`, executable path `dist/desktop/win-unpacked/WumingTown.exe`, unsigned unpacked scope and renderer chunk-size warning.
- Electron remains a platform shell and not simulation authority: e2e confirms `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, no renderer `process` or `require`, and unavailable placeholder save ports; report metadata records `simulationAuthority: "simulation-worker-or-headless"`.

## Checks run
| Command | Result | Artifact / notes |
|---|---|---|
| `pnpm build:desktop` | Pass | Ran twice consecutively; stable package digest `a88b28f6230ae2494fccccb9db44cebecc203bf1e7aae62011467bbde59b2f15`. |
| `pnpm test:e2e --filter desktop-shell` | Pass | 1 file, 2 tests. Verifies dev-server shell and packaged Windows directory launch. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Pass | 97 tasks, 10 roles. |
| `git diff --check` | Pass | No whitespace errors. |
| `pnpm quality` | Pass | Typecheck, lint, format check, content validation, boundaries check and 65 unit test files. |

## Determinism, save, performance and security impact
Determinism impact: none to simulation stepping, seeds, hashes or Worker protocol. The shell still consumes the reviewed M5 fixture as read-only product-gate evidence.

Save impact: no Windows save bridge was implemented. Electron preload still returns safe placeholder unavailable results, so the WM-0088 Windows/Web save-container interoperability blocker remains.

Performance impact: no benchmark threshold changed. Package report records directory size and renderer chunk-size warning for later product-gate consolidation.

Security impact: no new dependency or broad host API. Electron settings remain `contextIsolation=true`, `nodeIntegration=false`, `sandbox=true`; preload exposes only the existing whitelisted placeholder platform bridge.

## Decisions and deviations
- WM-0090 intentionally produces an unsigned unpacked directory, not an installer or signed release artifact.
- The package report is generated under ignored `dist/` and is not committed; source generation and e2e assertions are committed.
- `pnpm test:e2e --filter desktop-shell` emits Node `DEP0190` from the existing test harness spawning `pnpm` with `shell: true` on Windows. It is recorded as a known test-harness warning, not a packaged runtime warning.
- The renderer build emits the existing Vite chunk-size warning. WM-0090 records it and does not change loading thresholds.

## Remaining risks / follow-up tasks
- Windows/Web save-container interoperability remains blocked until a reviewed desktop save bridge lands.
- Electron security and preload API minimality are fully audited in downstream WM-0091.
- This task does not prove installer, signing, updater, Steam/store or public release readiness.

## Handoff to reviewer
Review `apps/desktop-electron/package-dir.ts`, `apps/desktop-electron/src/desktop-shell.e2e.test.ts`, `docs/05_tech/06_platform_matrix.md`, `coordination/tasks/WM-0090.json` and this report. Reproduce with:

```bash
pnpm build:desktop
pnpm test:e2e --filter desktop-shell
node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate
git diff --check
pnpm quality
```
