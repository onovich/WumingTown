# WM-0056 Work report

## Repair plan after independent review
- Remove requested horizon from checkpoint hash inputs so shared prefix
  checkpoints compare equal across `12000` and `20000` tick runs.
- Align primary seed contract and runtime authority by deriving scenario
  streams from `scenario.start` / primary seed `46`, while retaining the CLI
  seed as run metadata.
- Replace hard-coded terminal invariant zeroes with counters computed from the
  owned needs, mood, relationship, and explicit M4 absence surfaces, then add
  focused regression coverage for those derivations.

## Outcome
Implemented the executable M3 ordinary-life vertical scenario `m3-ordinary-life` / `m3.ordinary_life.injured_caregiver.v1` in `sim-core` and wired it into the headless CLI. The scenario composes the reviewed M3 owner stores for needs, environment, rest/sleep, food/eating, health/ability, medical care, mood/thoughts, relationships, reservations, jobs, hashes, bounded reason traces, queue metrics, replay evidence, and terminal invariant counters.

## Files and public contracts changed
- `packages/sim-core/src/m3-ordinary-life-scenario.ts`: new deterministic scenario fixture, command stream, owner-store composition, reason traces, hashes, queue/performance/invariant summaries, and replay evidence.
- `packages/sim-core/src/m3-ordinary-life-scenario.test.ts`: focused tests for determinism, injury consequences, structured reasons, hashes, metrics, and terminal invariants.
- `packages/sim-core/src/index.ts`: exports scenario runner, constants, and summary types.
- `tools/headless-runner/src/index.ts`: adds CLI scenario alias `m3-ordinary-life`.
- `docs/02_systems/17_m3_ordinary_life_scenario.md`: records WM-0056 executable scenario evidence while leaving save/Worker/benchmark/M4 gates downstream.
- `docs/07_roadmap/08_m3_ordinary_life_simulation_plan.md`: adds WM-0056 implementation note.
- `coordination/tasks/WM-0056.json`: taskctl claim metadata.
- `coordination/reports/WM-0056.md`: this report.

## Acceptance evidence
- Deterministic scenario id/seed/command stream/tick horizons: `pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 20000` emitted scenario id `m3.ordinary_life.injured_caregiver.v1`, authoritative seed `46`, requested seed `3`, primary seed `46`, tick rate `30`, short/full/long horizons `12000/36000/100000`, command stream hash `0x226832d2`, content hash `0xdfe7107e`, and world hash `0x7b37ff29`.
- Prefix-stable checkpoint hashes: `--ticks 12000` and `--ticks 20000` share identical checkpoint hashes at ticks `0`, `3600`, `7200`, and `12000`: `0xb19ad1c2`, `0xec4b052f`, `0x78877afa`, and `0x8249d7b6`. Focused Vitest now compares shared checkpoints across horizons.
- Injury visibly affects work, medical care, logistics, mood, and relationship state: focused test asserts Yao movement drops to `480`, treatment improves movement to `640`, treatment completes once, one bandage is consumed, one food portion is consumed, pain/weather mood traces are emitted, `yaoMinGratitude` and `yaoLinCare` are positive, and work rejections include `work.interrupted_by_ability_change`, `work.rejected_actor_ability_below_threshold`, and `work.rejected_outdoor_night_window`.
- Day/night/weather deterministic context: CLI evidence ends with `finalWeather: rain_light`, `finalScheduleWindow: night`, and reason trace `weather.changed_by_command`.
- Summary emits hashes, reason traces, queue metrics, terminal counters: CLI output includes checkpoint hashes at `0`, `3600`, `7200`, `12000`, `18000`; bounded reason traces; queue metrics; performance counters; replay hash match; zero terminal reservation/job leaks; and derived terminal invariant counters from owner stores: `needLaneCheckCount=30`, `moodLaneCheckCount=36`, `relationshipLaneCheckCount=15`, `m4AbsenceCheckCount=5`, with zero negative need lanes, mood/relationship range failures, and active M4 facts.
- No global scans/upstream bypasses: scenario uses owner-store indexes and capped selectors (`WorkOfferIndex`, `NeedUrgencyIndex`, food candidates, relationship recent-event selection) plus exact path count tracking; no apps, Worker protocol, save schema, new dependencies, benchmark baselines, or M4 systems were changed.

## Checks run
| Command | Result | Artifact / notes |
|---|---|---|
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | Passed | `Validation passed: 60 task(s), 10 role(s).` |
| `pnpm test --filter m3-ordinary-life-scenario` | Expected unsupported | Repository test runner reports `Unsupported unit test filter: m3-ordinary-life-scenario`; direct Vitest file used instead. |
| `pnpm exec vitest run ./packages/sim-core/src/m3-ordinary-life-scenario.test.ts --exclude=**/*.e2e.test.ts` | Passed | 1 file, 3 tests. |
| `pnpm typecheck` | Passed | `tsc --noEmit --project tsconfig.typecheck.json --pretty false`. |
| `pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 20000` | Passed | authoritative seed `46`, requested seed `3`, world hash `0x7b37ff29`; replay hash match true; terminal reservations/jobs both zero. |
| `pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 12000` | Passed | world hash `0x02c1d128`; shared checkpoint hashes through tick `12000` match the `20000` run. |
| `git diff --check` | Passed | No whitespace errors. |
| `pnpm quality` | Passed | typecheck, lint, format check, content validation, boundary check, and 34 unit files / 196 tests passed. |

## Determinism, save, performance and security impact
Determinism: uses the scenario contract primary seed `46` from `scenario.start`, records the CLI seed as `requestedSeed`, uses named random streams, integer/fixed tick semantics, stable command and candidate ordering, prefix-stable checkpoint hashes, and replay hash evidence.

Save: no public save format, schema, migration, or Worker protocol changes. The scenario emits checkpoint evidence only; focused save/load resume remains WM-0057.

Performance: no benchmark baseline update. The scenario summary reports deterministic counters: actor think passes, bounded candidate visits, exact path request count, queue peaks, and cap hits.

Security/scope: no new dependencies, no DOM/React/Pixi/Electron/Node filesystem/real-time authority in sim-core, no apps/sim-worker edits, no M4 lamp/Chronicle/anomaly/crisis/director scope.

## Decisions and deviations
ADR-0008 was followed: owner stores remain authoritative, derived indexes/caches are rebuilt or invalidated from owner facts, job progress stays in explicit job drivers, and candidate selection is bounded. The only test-path deviation is that `pnpm test --filter m3-ordinary-life-scenario` is unsupported by the repository test runner, so the focused direct Vitest file was used and documented.

## Remaining risks / follow-up tasks
- WM-0057 still needs focused save/load/resume over the approved M3 owner-store scope.
- WM-0058 still needs Worker/headless parity; this task only wires the Node/headless CLI.
- WM-0059 still needs reviewed long-run benchmarks and baselines; this task only emits scenario metrics.

## Handoff to reviewer
Fastest reproduction path:

1. `pnpm exec vitest run ./packages/sim-core/src/m3-ordinary-life-scenario.test.ts --exclude=**/*.e2e.test.ts`
2. `pnpm sim:run -- --seed 3 --scenario m3-ordinary-life --ticks 20000`
3. `pnpm quality`

Inspect `packages/sim-core/src/m3-ordinary-life-scenario.ts` for the command stream and owner-store composition, and `tools/headless-runner/src/index.ts` for the CLI alias integration.
