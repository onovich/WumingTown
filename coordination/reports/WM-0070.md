# WM-0070 Work report

## Outcome

Added an artifact-backed M4 long-run invariant and benchmark gate for
`m4.core_vertical_slice.borrowed_shadow_lamps.v1` without changing product
behavior, public save/protocol/schema contracts, UI authority, dependencies or
M5 scope. The M4 benchmark is in the default suite and preserves 10 percent
warning / 20 percent blocking thresholds.

## Files and public contracts changed

- `packages/sim-core/src/m4-core-vertical-slice-scenario.ts`: added read-only
  `performanceMetrics` summary derived from existing M4 owner-store metrics.
- `packages/sim-core/src/m4-invariants.test.ts`: added M4 long-run invariant
  gate through `100000` ticks, save/replay parity and projection hash checks.
- `tools/test-runner.mjs`: added `m4-invariants` filter and included existing
  M4 Worker parity test in that gate.
- `packages/benchmarks/src/m4-core-vertical-slice-long-run-benchmark.ts` and
  benchmark registry files: added M4 long-run benchmark, invariant extraction,
  default-suite registration and exports.
- `packages/benchmarks/src/cli-lib.ts`: added M4 baseline parsing/filtering,
  WM-0070 default artifact routing and `reviewedSha256` artifact field.
- `packages/benchmarks/baseline.json`: added M4 benchmark baseline from the
  reviewed WM-0070 artifact; warning/blocking thresholds remain `10` / `20`.
- `docs/05_tech/03_performance_budget.md` and
  `docs/05_tech/05_testing_observability_ci.md`: documented the M4 benchmark,
  invariant gate and artifact hash.
- `coordination/artifacts/WM-0070/benchmarks/benchmark-results.json`: reviewed
  benchmark artifact.

## Acceptance evidence

- Long-run invariant gate samples ticks `12000`, `36000`, `60000`, `80000` and
  `100000`; the post-`36000` idle window asserts no lamp dirty backlog growth,
  evidence drift, dissemination backlog growth or obligation leaks.
- Existing M4 Worker parity test is included under `pnpm test --filter
  m4-invariants`, catching Worker projection mismatch without adding new public
  Worker protocol.
- Benchmark reports lamp dirty backlog peak/final counts, active gaps,
  evidence support candidates, confirmed rules, dissemination backlog,
  obligation due/violation counts, town-rule candidates, crisis transitions,
  director candidates/recovery windows, ReasonTrace capacity/use, save/load
  rebuilt surface count, Worker projection bytes and final hashes.
- Benchmark artifact path:
  `coordination/artifacts/WM-0070/benchmarks/benchmark-results.json`.
- Reviewed SHA-256:
  `2E537A601FE41868C02E89D57322318F88841F471DF20749638FC17FF0674DE3`.
- M4 final world/read-model hashes: `0xdafa3b25` / `0x08dd9343`.
- Artifact environment includes Node `v24.13.1`, pnpm `11.8.0`, Windows
  `10.0.26200`, CPU `AMD Ryzen 7 9700X 8-Core Processor`, CPU count `16`, and
  git commit `03f33c319e03d1aa00c42a58ae03042ba1ff1f21`.

## Checks run

| Command | Result | Artifact / notes |
| --- | --- | --- |
| `cmd /c pnpm.cmd typecheck` | PASS | `tsc --noEmit --project tsconfig.typecheck.json --pretty false` |
| `cmd /c pnpm.cmd test --filter m4-invariants` | PASS | 2 files, 3 tests; includes M4 Worker parity |
| `cmd /c pnpm.cmd bench --filter m4-core-vertical-slice-long-run` | PASS | Initial M4-only artifact, median `4.560600000000022` ms |
| `cmd /c pnpm.cmd bench` | FAIL then PASS on rerun | First full-suite run had transient `entity-store` median `330.840` ms; isolated `entity-store` rerun passed at `251.873` ms; full-suite rerun passed and produced the reviewed artifact above |
| `cmd /c pnpm.cmd format:check` | PASS | All matched files use Prettier style |
| `cmd /c pnpm.cmd lint` | FAIL then PASS | Initial failure in new `m4-invariants.test.ts` (`expect.arrayContaining` typing and array `filter`); fixed and reran successfully |
| `cmd /c pnpm.cmd typecheck` | PASS | Rerun after lint fix |
| `cmd /c pnpm.cmd test --filter m4-invariants` | PASS | Rerun after lint fix; 2 files, 3 tests |
| `cmd /c pnpm.cmd format:check` | PASS | Rerun after lint fix |
| `cmd /c pnpm.cmd sim:run -- --seed 4 --scenario m4-core-vertical-slice --ticks 100000` | PASS | Headless summary final world hash `0xdafa3b25`; scenario read-model hash `0xa896439d` |
| `cmd /c pnpm.cmd quality` | PASS | Includes typecheck, lint, format check, content validation, boundary check and all unit tests: 50 files, 275 tests |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | PASS | 71 tasks, 10 roles |
| `git diff --check` | PASS | No whitespace errors |

## Determinism, save, performance and security impact

Determinism: M4 replay checkpoints include the `100000` tick horizon and match
the benchmark final hash. Save/replay: focused M4 save at `12000`, load at
`12001`, and resume through `100000` match uninterrupted replay. Performance:
only benchmark/invariant measurement and baseline data changed; thresholds
remain unchanged. Security/public contracts: no dependency, schema, public save,
public Worker protocol, UI authority or Electron changes.

## Decisions and deviations

ADR-0009 remains the architecture gate. The implementation intentionally
derives M4 metrics from existing owner-store `createMetrics()` surfaces instead
of changing M4 product behavior. A first full `pnpm bench` failure was isolated
to a noisy pre-existing `entity-store` timing sample; no baseline was changed
for that benchmark, and the full suite passed on rerun.

## Remaining risks / follow-up tasks

No unresolved blockers. The artifact `gitCommit` records the pre-commit HEAD
because benchmarks run before task completion commit creation.

## Handoff to reviewer

Fast path: inspect
`coordination/artifacts/WM-0070/benchmarks/benchmark-results.json`, verify
`reviewedSha256`, then run `pnpm test --filter m4-invariants`, `pnpm bench` and
`pnpm sim:run -- --seed 4 --scenario m4-core-vertical-slice --ticks 100000`.
