# WM-0030 Work report

## Outcome

WM-0030 closes the M1 simulation-kernel gate as a coordination and evidence
task. It documents the reviewed M1 task chain, records the final closeout
checks, refreshes the benchmark artifact produced during closeout, and prepares
the project for a future M2 planning step without starting M2 implementation.

## Files and public contracts changed

- `coordination/project-state.json`: records the project as being in M1
  closeout and states that M2 is not started.
- `docs/07_roadmap/00_roadmap.md`: adds the M1 closeout status and M2 entry
  guard.
- `docs/07_roadmap/01_milestones_and_quality_gates.md`: records M1 closeout
  evidence, residual risks and nonblocking warnings.
- `docs/07_roadmap/06_m1_simulation_kernel_plan.md`: marks the M1 plan closed
  by WM-0030 and records the gate evidence.
- `coordination/artifacts/WM-0010/determinism/`: stores replay diagnostics
  generated by `pnpm ci:local` during closeout.
- `coordination/artifacts/WM-0010/environment/`: stores tool/environment
  metadata generated by `pnpm ci:local`.
- `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json`: refreshes
  the final M1 benchmark comparison artifact for the closeout run.
- `coordination/reports/WM-0030-m2-entry-prompt.md`: future M2 entry prompt and
  criteria; it is not an instruction to start M2 in this task.

No runtime API, save schema, Worker protocol or simulation behavior changed in
WM-0030.

## Acceptance evidence

- Headless 100k tick hauling/building scenario is reproducible and reviewed:
  `pnpm sim:run -- --seed 1 --scenario hauling-building --ticks 100000` passed
  with `worldHash=0xf7815189`, `longRunStable=true`,
  `idleWindow.sampleCount=11`, first idle sample tick `2401`, last idle sample
  tick `100000`, and zero max demand/build/active-offer/reservation/running-job
  counts across the idle window.
- Browser Worker and Node produce the same authoritative hash for the M1
  scenario: `pnpm test:e2e --filter worker-smoke` passed, including the M1
  parity coverage added by WM-0028.
- 50k empty or inert entities show no sustained queue or memory growth:
  `pnpm bench` passed the spatial-index pressure benchmark with
  `entityCount=50000`, `finalBacklogCount=0`, `finalMapMemberships=49488`, and
  `finalIndexedEntities=49488`. The benchmark path records heap deltas; the
  durable JSON artifact records the invariant fields used by closeout.
- M1 residual risks, benchmark artifacts, save/replay impact and next M2 entry
  criteria are documented in this report,
  `docs/07_roadmap/01_milestones_and_quality_gates.md`,
  `coordination/artifacts/WM-0010/determinism/replay-diagnostics-summary.json`,
  `coordination/artifacts/WM-0029/benchmarks/benchmark-results.json`, and
  `coordination/reports/WM-0030-m2-entry-prompt.md`.

## Checks run

| Command | Result | Artifact / notes |
|---|---|---|
| `pnpm install --frozen-lockfile` | PASS | Lockfile accepted without dependency changes. |
| `pnpm ci:local` | PASS | 22 unit files / 107 tests passed; 3 E2E files / 11 tests passed; replay diagnostics passed. Existing Node `DEP0190` warning observed. |
| `pnpm build:web` | PASS | Vite build passed; existing chunk-size warning observed. |
| `pnpm build:desktop` | PASS | Electron package build passed; existing Vite chunk-size warning observed. |
| `pnpm sim:run -- --seed 1 --scenario hauling-building --ticks 100000` | PASS | Final world hash `0xf7815189`; `longRunStable=true`. |
| `pnpm bench` | PASS | M1 hauling/building and spatial-index benchmark comparisons passed; artifact refreshed. |
| `pnpm test:e2e --filter worker-smoke` | PASS | Additional acceptance evidence for Worker/Node parity. |
| `node tools/validate-handoff.mjs` | PASS | 532 files, 30 tasks, 10 roles; command emitted the current structure digest. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs validate` | PASS | 30 tasks and 10 roles validated. |
| `node .agents/skills/wuming-town-agent-workflow/scripts/taskctl.mjs status` | PASS | 1 in-progress task, 29 done tasks, 0 unread inbox messages. |
| `git diff --check` | PASS | No whitespace errors. |
| `pnpm format:check` | PASS | Prettier check passed for matched files, including the milestone quality-gate doc. |

## Determinism, save, performance and security impact

Determinism: WM-0030 changes no simulation code. It records the 100000-tick M1
hash, Worker/headless parity and replay diagnostics generated from the current
branch.

Save: WM-0030 changes no save format. The closeout evidence includes M1
save-interrupt-load-resume diagnostics and keeps public save compatibility
outside M1 scope.

Performance: WM-0030 changes no hot path. It refreshes the M1 benchmark
artifact and records that the existing 10 percent warning and 20 percent
blocking regression thresholds still pass.

Security: WM-0030 changes no Electron, preload, IPC, Worker authority or
dependency surface.

## Decisions and deviations

No ADR is changed by WM-0030. The only scope addition beyond the required check
list is `pnpm test:e2e --filter worker-smoke`, run to make the Worker/Node
parity acceptance criterion directly reproducible in the closeout branch.

## Remaining risks / follow-up tasks

- M1 save/replay covers the focused M1 envelope; broader public save
  compatibility, migrations and platform save UI remain future work.
- M1 proves the first hauling/building simulation kernel slice, not broad
  economy, town-life, anomaly, relationship or content systems.
- Product-scale memory soaks beyond the current 50000-entity pressure benchmark
  remain an M2+ performance responsibility.
- Existing Node `DEP0190` and Vite chunk-size warnings are nonblocking but
  should remain visible in later build/CI cleanup.

## Handoff to reviewer

Start from branch `task/WM-0030-close-the-m1-simulation-kernel-gate`.
Review the documentation-only diff plus generated closeout artifacts, then
rerun the required checks in this report. Confirm that the M1 gate is closed
without starting M2, and that the future M2 entry prompt is only preparatory.
