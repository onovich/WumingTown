# ADR-0002: Pinned M0 tool versions

- Status: Accepted
- Date: 2026-06-23
- Owners: systems-architect
- Related tasks: WM-0002

## Context

WM-0002 must turn the approved stack into reproducible quality gates. Versions were verified against npm registry metadata on 2026-06-23 and are pinned exactly where installed.

## Decision drivers

- Correctness and determinism
- Performance and scale
- AI/human development efficiency
- Web/Windows portability
- Save and mod compatibility
- Security and rollback cost

## Considered options

- Floating major ranges: convenient upgrades, but violates the dependency security policy.
- Pin only TypeScript and pnpm: smaller lockfile, but lint/test behavior could drift.
- Pin all installed M0 tools exactly: stronger reproducibility with small maintenance overhead.

## Decision

Pin the installed M0 toolchain as follows:

| Tool                     |     Version | Location                                      |
| ------------------------ | ----------: | --------------------------------------------- |
| Node.js                  | 24 LTS line | `engines.node`, CI `node-version: "24"`       |
| pnpm                     |      11.8.0 | `packageManager`, `engines.pnpm`, CI Corepack |
| TypeScript               |       5.9.3 | root `devDependencies`                        |
| Vite                     |       8.1.0 | root `devDependencies`                        |
| Vitest                   |       4.1.9 | root `devDependencies`                        |
| ESLint                   |      10.5.0 | root `devDependencies`                        |
| `@eslint/js`             |      10.0.1 | root `devDependencies`                        |
| `typescript-eslint`      |      8.62.0 | root `devDependencies`                        |
| Prettier                 |       3.8.4 | root `devDependencies`                        |
| `eslint-config-prettier` |      10.1.8 | root `devDependencies`                        |
| `@types/node`            |     24.13.2 | root `devDependencies`                        |

The architecture-selected runtime frameworks are not installed in WM-0002 because no WM-0002 package consumes them yet. Registry-resolvable candidates recorded for later tasks are React `19.2.7`, React DOM `19.2.7`, PixiJS `8.19.0`, Electron `42.4.1`, and `@vitejs/plugin-react` `6.0.3`. The future task that first imports each runtime framework must add it exactly, update the lockfile, and extend this ADR or create a superseding ADR with dependency review evidence.

## WM-0003 addendum: web shell dependency review

- Date: 2026-06-24
- Related tasks: WM-0003
- Reviewer follow-up: dependency policy evidence for React/Pixi/Playwright stack and `skipLibCheck` blast radius

### Context

WM-0003 is the first task that imports browser UI/runtime packages from the approved stack. `docs/06_engineering/06_dependency_security_policy.md` requires new dependency evidence covering function, alternatives, maintenance, license, browser/Electron compatibility, package size, security history, and install-script review. WM-0003 also introduced a TypeScript lib-check workaround while importing `pixi.js@8.19.0`; the reviewer requested either a narrower scope or explicit justification.

Pins are exact in workspace manifests and lockfile:

- `apps/web/package.json`: `react@19.2.7`, `react-dom@19.2.7`
- `packages/renderer-pixi/package.json`: `pixi.js@8.19.0`
- root `package.json`: `playwright@1.61.1`, `@types/react@19.2.17`, `@types/react-dom@19.2.3`
- `pnpm-lock.yaml` contains concrete entries for each reviewed version, and `pnpm-workspace.yaml` records matching `minimumReleaseAgeExclude` pins for the WM-0003 additions.

### Dependency review

| Package                   | Function in WM-0003                                                                | Alternatives considered                                                                                                                                                                  | Maintenance evidence                                                                       | License    | Compatibility                                                                                                      | Registry unpacked size | Security / install-script review                                                                                                                                                                                                                                 | Decision                                      |
| ------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------: | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `react@19.2.7`            | Declarative HUD and inspector rendering in `packages/ui-react`                     | DOM strings/manual store wiring, Preact, Solid. Rejected because the architecture already reserves React for UI overlays and the repo now needs the standard ecosystem/tooling path.     | npm metadata points to `facebook/react`; registry metadata modified `2026-06-23`.          | MIT        | Browser runtime; no Electron-only surface required.                                                                |          171,604 bytes | `pnpm audit --json` reported 0 advisories. No `preinstall` / `install` / `postinstall` lifecycle scripts declared.                                                                                                                                               | Accept                                        |
| `react-dom@19.2.7`        | Browser mount path via `createRoot` in `apps/web/src/shell-bootstrap.ts`           | Manual DOM patching or alternate renderer layer. Rejected because React without `react-dom` does not satisfy the browser shell composition requirement.                                  | Same React upstream repository; registry metadata modified `2026-06-23`.                   | MIT        | Browser runtime; mounts cleanly in the Vite/Chromium shell and does not require Electron privileges.               |        7,319,413 bytes | `pnpm audit --json` reported 0 advisories. No `preinstall` / `install` / `postinstall` lifecycle scripts declared.                                                                                                                                               | Accept                                        |
| `pixi.js@8.19.0`          | Chunked world canvas rendering and pointer input in `packages/renderer-pixi`       | Raw Canvas 2D, WebGL from scratch, Phaser. Rejected because the architecture explicitly assigns world rendering to Pixi and WM-0003 needs sprite/canvas primitives without per-cell DOM. | npm metadata points to `pixijs/pixijs`; registry metadata modified `2026-06-05`.           | MIT        | Browser-focused renderer package compatible with Chromium web shell; no Electron-specific APIs used.               |       72,415,382 bytes | `pnpm audit --json` reported 0 advisories. No `preinstall` / `install` / `postinstall` lifecycle scripts declared. Manifest contains `prepare`, but that is a publish/development hook rather than an install-time lifecycle used by this workspace.             | Accept with scoped TypeScript exception below |
| `playwright@1.61.1`       | Deterministic Chromium smoke automation for `pnpm test:e2e`                        | Vitest DOM mocks only, Puppeteer, manual smoke. Rejected because WM-0003 acceptance requires a real Chromium screenshot artifact.                                                        | npm metadata points to `microsoft/playwright`; registry metadata modified `2026-06-23`.    | Apache-2.0 | Dev/test-only on Node `>=18`; runs in this Node 24 toolchain and drives Chromium without touching product runtime. |        4,875,435 bytes | `pnpm audit --json` reported 0 advisories. No package `preinstall` / `install` / `postinstall` lifecycle scripts declared; browser download is an explicit command in `pnpm test:e2e` via `pnpm exec playwright install chromium`, not an implicit install hook. | Accept                                        |
| `@types/react@19.2.17`    | Strict TS surface for React hooks/JSX-free element creation in `packages/ui-react` | Hand-written ambient declarations or looser `unknown` shims. Rejected because they weaken strictness and duplicate maintained upstream community types.                                  | Published from `DefinitelyTyped/DefinitelyTyped`; registry metadata modified `2026-06-05`. | MIT        | Type-only package; browser/Electron neutral.                                                                       |          407,653 bytes | `pnpm audit --json` reported 0 advisories. No install lifecycle scripts declared.                                                                                                                                                                                | Accept                                        |
| `@types/react-dom@19.2.3` | Strict TS surface for `react-dom/client` mount API                                 | Hand-written declarations or `any` escape hatches. Rejected for the same strictness reasons as `@types/react`.                                                                           | Published from `DefinitelyTyped/DefinitelyTyped`; registry metadata modified `2025-11-12`. | MIT        | Type-only package; browser/Electron neutral.                                                                       |           30,063 bytes | `pnpm audit --json` reported 0 advisories. No install lifecycle scripts declared.                                                                                                                                                                                | Accept                                        |

### TypeScript lib-check exception

WM-0003 keeps a `skipLibCheck` exception only in `tsconfig.typecheck.json`, not in `tsconfig.base.json`, to reduce blast radius to the aggregate owner gate. The reviewer probe was reproduced on 2026-06-24 with:

```bash
pnpm exec tsc --noEmit --project tsconfig.typecheck.json --pretty false --skipLibCheck false
```

Observed failure:

```text
node_modules/.pnpm/pixi.js@8.19.0/node_modules/pixi.js/lib/scene/text-bitmap/AbstractBitmapFont.d.ts(80,31): error TS2420: Class 'AbstractBitmapFont<FontType>' incorrectly implements interface 'Omit<BitmapFontData, "fontSize" | "chars" | "pages">'.
```

Rationale and blast radius:

- The exception is now scoped to the root aggregate `pnpm typecheck` path instead of every project extending `tsconfig.base.json`.
- WM-0003 source strictness remains unchanged: `strict`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, and other local rules still apply to repository code.
- This does not suppress lint, unit, or e2e validation, and it does not relax runtime boundary checks.

Revisit trigger:

- Remove the exception once a pinned Pixi release resolves the declaration issue, or once the repository adopts a narrower local declaration patch with equivalent reproducibility evidence.

## Consequences

### Positive

- Root gates are reproducible from `pnpm-lock.yaml`.
- CI and local setup use the same pnpm line.
- Runtime packages with heavier install/security footprint are not introduced before they are used.

### Negative

- CI uses the Node 24 LTS line rather than a patch-level Node binary pin.
- Future UI/Electron tasks must perform their own dependency security review before first import.

### Risks and mitigations

- Risk: Node patch drift changes behavior. Mitigation: keep source code platform-neutral and add patch-level pinning when packaging/runtime smoke tasks begin.
- Risk: toolchain upgrades pile up. Mitigation: batch upgrades through dependency policy and rerun all root gates.

## Contracts introduced

- External npm dependency versions must be exact; no `latest`, `*`, `^`, or `~`.
- Internal workspace dependencies use `workspace:0.0.0`.
- Root gates are `typecheck`, `lint`, `format:check`, `test`, `content:validate`, and `boundaries:check`.

## Verification

- `pnpm install --frozen-lockfile`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm format:check`
- `pnpm test`
- `pnpm content:validate`
- `pnpm boundaries:check`

## Rollback / migration

Revert root package/config files and regenerate the lockfile before any product runtime state depends on this toolchain.
